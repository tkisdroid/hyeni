---
phase: 01-migration-hygiene-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/down/README.md
  - supabase/migrations/down/.gitkeep
autonomous: true
requirements: []
tags: [migration-hygiene, supabase, rollback, convention]

must_haves:
  truths:
    - "`supabase/migrations/down/` exists as a tracked directory in git"
    - "`supabase/migrations/down/README.md` declares the up↔down pairing convention"
    - "`supabase/migrations/down/README.md` declares the BEGIN/COMMIT transactional wrap convention"
    - "The README explicitly states that retroactive pairing for pre-Phase-1 up migrations is OUT OF SCOPE"
  artifacts:
    - path: "supabase/migrations/down/README.md"
      provides: "Rollback convention declaration for all future migrations"
      contains: "BEGIN"
    - path: "supabase/migrations/down/.gitkeep"
      provides: "Ensures directory is tracked by git even when empty of .sql files"
  key_links:
    - from: "supabase/migrations/down/README.md"
      to: "supabase/migrations/"
      via: "filename pairing rule"
      pattern: "YYYYMMDDHHMMSS"
---

<objective>
Create `supabase/migrations/down/` directory and populate it with a README that declares two conventions enforced for every migration authored from Phase 1 forward:

1. **Up↔Down pairing rule:** Every new `supabase/migrations/YYYYMMDDHHMMSS_name.sql` authored from 2026-04-21 onward MUST ship a matching `supabase/migrations/down/YYYYMMDDHHMMSS_name.sql` with the exact reverse DDL (DROP COLUMN, DROP POLICY, DROP TABLE, DROP PUBLICATION MEMBER, etc.).

2. **Transactional wrap rule:** Every migration that contains DROP POLICY + CREATE POLICY sequences (or any multi-statement DDL that must apply atomically) MUST be wrapped in `BEGIN; ... COMMIT;` with `ROLLBACK;` on exception.

This is the last of three PITFALLS.md line 321 pre-conditions to eliminate: "The repo currently has no down/ directory — creating one is a pre-condition for P0-2."

Purpose: Guarantees that every live-data SQL change from Phase 2 onward has a verified rollback artifact before main promotion.

Output: Two new files (README + .gitkeep). Zero changes to existing up-migrations. Retroactive backfill for the 15 existing up-migrations is explicitly deferred (CONTEXT.md D-03 defers this; the README declares the deferral in writing).

Implements D-03, D-04 from CONTEXT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-migration-hygiene-baseline/01-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `supabase/migrations/down/` directory + `.gitkeep` stub</name>
  <files>
    supabase/migrations/down/.gitkeep
  </files>
  <read_first>
    - supabase/migrations (directory listing — confirm no `down/` subdir exists today)
    - .planning/phases/01-migration-hygiene-baseline/01-CONTEXT.md (§D-03)
    - .planning/research/PITFALLS.md (line 321: "repo currently has no down/ directory")
  </read_first>
  <action>
    Run from the repo root:

    ```
    mkdir -p supabase/migrations/down
    printf '' > supabase/migrations/down/.gitkeep
    ```

    The `.gitkeep` file is a zero-byte placeholder that guarantees git tracks the directory even before the first `down/*.sql` file ships (which happens in Plan 03). Do NOT substitute a `.keep` or dotfile variant — `.gitkeep` is the long-standing community convention and matches what tools like `git-scm` recognize.

    Do NOT put a placeholder `.sql` file here. Do NOT edit any file in `supabase/migrations/` — this plan touches only `down/`.
  </action>
  <verify>
    <automated>bash -c "test -d supabase/migrations/down && test -f supabase/migrations/down/.gitkeep && test ! -s supabase/migrations/down/.gitkeep"</automated>
  </verify>
  <acceptance_criteria>
    - Directory `supabase/migrations/down/` exists.
    - File `supabase/migrations/down/.gitkeep` exists and is zero bytes.
    - `git status` shows `supabase/migrations/down/.gitkeep` as a new tracked file (no other files changed).
    - `supabase/migrations/*.sql` (the 15 existing up-migrations) are unchanged — `git diff supabase/migrations/*.sql` is empty.
  </acceptance_criteria>
  <done>Directory created and committed to git via `.gitkeep`; subsequent plans can write `supabase/migrations/down/YYYYMMDDHHMMSS_*.sql` files into it without a pre-step.</done>
</task>

<task type="auto">
  <name>Task 2: Author `supabase/migrations/down/README.md` declaring pairing + transaction conventions</name>
  <files>supabase/migrations/down/README.md</files>
  <read_first>
    - supabase/migrations/20260418000000_family_subscription.sql (example of an existing up migration)
    - supabase/migrations/20260418000005_subscription_rls.sql (example of a policy-heavy up migration)
    - .planning/phases/01-migration-hygiene-baseline/01-CONTEXT.md (§D-03, §D-04)
    - .planning/research/ARCHITECTURE.md §3.2 (rollback discipline per SQL item)
    - .planning/research/PITFALLS.md §"down-migration authorship"
  </read_first>
  <action>
    Create `supabase/migrations/down/README.md` with the following exact content (the convention declaration is normative — word it precisely):

    ```markdown
    # supabase/migrations/down/ — Rollback Migration Convention

    Established: 2026-04-21 (Phase 1 — Migration Hygiene & Baseline, per D-03 / D-04).
    Canonical source: `.planning/phases/01-migration-hygiene-baseline/01-CONTEXT.md`.

    ## Rule 1 — Up ↔ Down pairing (mandatory from 2026-04-21 forward)

    For every new forward migration authored as:

    ```
    supabase/migrations/YYYYMMDDHHMMSS_<name>.sql
    ```

    the author MUST simultaneously ship a matching reverse migration at:

    ```
    supabase/migrations/down/YYYYMMDDHHMMSS_<name>.sql
    ```

    The **filename must match byte-for-byte** (same timestamp, same name, same
    extension). The down file must reverse the up file with the exact inverse
    DDL:

    | Up operation | Down operation |
    |--------------|----------------|
    | `CREATE TABLE foo` | `DROP TABLE foo` |
    | `ALTER TABLE foo ADD COLUMN bar` | `ALTER TABLE foo DROP COLUMN bar` |
    | `CREATE POLICY p ON foo ...` | `DROP POLICY IF EXISTS p ON foo` |
    | `ALTER PUBLICATION supabase_realtime ADD TABLE foo` | `ALTER PUBLICATION supabase_realtime DROP TABLE foo` |
    | `CREATE INDEX idx_foo ON foo(bar)` | `DROP INDEX IF EXISTS idx_foo` |
    | `CREATE OR REPLACE FUNCTION f()` | `DROP FUNCTION IF EXISTS f()` *(or restore prior `CREATE OR REPLACE FUNCTION f()` body)* |

    Use `IF EXISTS` on every DROP in the down file so partial rollbacks do not
    error out mid-script.

    ## Rule 2 — Transactional wrap (`BEGIN;` / `COMMIT;` / `ROLLBACK;`)

    Every up and every down migration that contains **more than one DDL
    statement** MUST wrap the body in:

    ```sql
    BEGIN;

    -- ... statements ...

    COMMIT;
    ```

    This is non-negotiable for:

    - Any `DROP POLICY ... ; CREATE POLICY ...` sequence (there is otherwise
      a window where the table is fully open).
    - Any `ALTER TABLE ... ADD COLUMN ...; UPDATE ... SET <col> = ...;` backfill
      (partial apply leaves NULLs that break subsequent statements).
    - Any publication change with a companion `NOTIFY pgrst, 'reload schema';`
      (the NOTIFY must be inside the same transaction as the publication change).

    Single-statement migrations (e.g., a lone `ALTER PUBLICATION ... ADD TABLE`)
    do not strictly require the wrap, but wrapping them anyway is encouraged
    for uniform readability.

    ## Rule 3 — Idempotency guards

    Prefer `IF NOT EXISTS` on up operations and `IF EXISTS` on down operations
    so repeated application on a Supabase branch does not error. Example:

    ```sql
    ALTER TABLE public.memos
      ADD COLUMN IF NOT EXISTS created_at timestamptz DEFAULT now();
    ```

    ## Scope declaration — what this convention does NOT do

    **Retroactive backfill of down files for the 15 existing up migrations
    authored before 2026-04-21 is explicitly OUT OF SCOPE for Phase 1** (per
    CONTEXT.md D-03). The following up-migrations currently have **no** paired
    down file, and may remain unpaired until a specific rollback need arises:

    - `20260313000000_push_tables.sql`
    - `20260313000001_add_phone_columns.sql`
    - `20260314000002_native_push_support.sql`
    - `20260315135737_add_academy_schedule.sql`
    - `20260315152655_memo_replies_setup.sql`
    - `20260315152758_fix_memo_rls_for_child.sql`
    - `20260317000000_audio_recording_tier.sql`
    - `20260317100000_danger_zones_sticker_update.sql`
    - `20260418000000_family_subscription.sql`
    - `20260418000001_subscription_tier_cache.sql`
    - `20260418000002_active_slot_columns.sql`
    - `20260418000003_subscription_soft_lock.sql`
    - `20260418000004_subscription_notifications.sql`
    - `20260418000005_subscription_rls.sql`
    - `20260418000006_saved_places.sql`

    If a rollback of one of the above is ever needed, the author must write
    the missing down file at that time and place it in this directory.

    ## Example (template for all future migrations)

    **Up:** `supabase/migrations/20260422103000_example.sql`
    ```sql
    BEGIN;
    ALTER TABLE public.example ADD COLUMN IF NOT EXISTS extra_col text;
    CREATE POLICY example_read ON public.example
      FOR SELECT USING (true);
    COMMIT;
    ```

    **Down:** `supabase/migrations/down/20260422103000_example.sql`
    ```sql
    BEGIN;
    DROP POLICY IF EXISTS example_read ON public.example;
    ALTER TABLE public.example DROP COLUMN IF EXISTS extra_col;
    COMMIT;
    ```

    Note: DROP operations in the down file MUST appear in **reverse order** of
    the up file — policies before the columns they reference, indexes before
    the tables they index, etc.
    ```

    Length target: ~150 lines of markdown. The README is the binding convention document — err on the side of precision over brevity. Do NOT embed production table names (e.g., `memos`, `pair_code`) except in the neutral "Rule 2" examples above — the README is a generic convention, not a schema document.

    Do NOT mention secrets, environment variables, or service-role keys anywhere in this file.
  </action>
  <verify>
    <automated>bash -c "test -f supabase/migrations/down/README.md && grep -q 'Up .. Down pairing' supabase/migrations/down/README.md && grep -q 'BEGIN;' supabase/migrations/down/README.md && grep -q 'COMMIT;' supabase/migrations/down/README.md && grep -q 'OUT OF SCOPE' supabase/migrations/down/README.md && grep -c '20260' supabase/migrations/down/README.md | awk '{exit ($1 < 15)}'"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/down/README.md` exists and is non-empty.
    - README contains the literal strings `BEGIN;`, `COMMIT;`, and `ROLLBACK` (or an explicit mention of rollback semantics).
    - README declares the pairing rule with the literal phrase `Up ↔ Down pairing` (or equivalent unambiguous phrasing containing both `Up` and `Down` and `pairing`).
    - README explicitly calls out `OUT OF SCOPE` for retroactive backfill (or `NOT in scope` / `not required` for pre-2026-04-21 migrations).
    - README enumerates all 15 existing unpaired up-migration filenames (`grep -c '20260' ... ≥ 15`).
    - README contains at minimum one inverse-DDL table (CREATE → DROP, ALTER ADD → ALTER DROP, CREATE POLICY → DROP POLICY).
    - File contains no secrets: `grep -E 'VAPID_PRIVATE|SERVICE_ROLE|eyJ'` returns nothing.
  </acceptance_criteria>
  <done>Convention README authored, committed; all future migration authors (Plan 03 is the first consumer) have a single normative document to reference.</done>
</task>

</tasks>

<verification>
After both tasks:

```
# 1. Directory + stub
test -d supabase/migrations/down
test -f supabase/migrations/down/.gitkeep
test ! -s supabase/migrations/down/.gitkeep

# 2. README exists with mandatory markers
test -f supabase/migrations/down/README.md
grep -q 'BEGIN;'       supabase/migrations/down/README.md
grep -q 'COMMIT;'      supabase/migrations/down/README.md
grep -q 'pairing'      supabase/migrations/down/README.md
grep -q 'OUT OF SCOPE' supabase/migrations/down/README.md

# 3. All 15 existing migrations named in the deferred list
test "$(grep -c '20260' supabase/migrations/down/README.md)" -ge 15

# 4. Existing up-migrations untouched
git status supabase/migrations/*.sql   # expect: clean
```

Branch verification / Playwright: NOT in this plan's scope. No SQL is authored here and no runtime behavior changes — the README is purely a convention declaration. The first actual down-migration file authored per this convention is Plan 03; end-to-end branch + Playwright verification runs in Plan 05.
</verification>

<success_criteria>
- `supabase/migrations/down/` directory present and tracked.
- `supabase/migrations/down/README.md` declares pairing + BEGIN/COMMIT + idempotency conventions.
- README explicitly scopes OUT the retroactive backfill for the 15 pre-existing up-migrations.
- Zero changes to existing up-migration files.
- D-03 and D-04 from CONTEXT.md implemented exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-migration-hygiene-baseline/01-02-SUMMARY.md` per the template.
</output>
