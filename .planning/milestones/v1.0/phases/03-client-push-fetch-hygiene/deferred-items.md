# Phase 3 — Deferred Items

Out-of-scope issues discovered during execution. NOT fixed this phase
per GSD scope boundary rule ("only auto-fix issues DIRECTLY caused by
the current task's changes").

| Category | Item | Discovered at | Notes |
|----------|------|--------------|-------|
| Test failure (pre-existing) | `tests/entitlementCache.test.js > reads back a cached value before ttl expires` — returns null instead of expected payload | Task 5 verification run | File not touched by Phase 3. Likely TTL/localStorage mock regression from an earlier phase. Assign to a future test-hygiene phase. |
| ESLint errors (pre-existing) | 18 errors in `src/App.jsx` (react-hooks/set-state-in-effect at L5180/L5717, react-hooks/refs at L6762, etc.) | Task 3 lint probe | None overlap with Phase 3 edit ranges (L93-143, L3952, L4425-4432, L4601-4614, L5949-5971). Monolith decomposition policy forbids refactoring during v1.0. |
| Test artifacts | `.reports/hyeni-*.png` Playwright screenshots found untracked at phase start | Task 5 git status | Pre-existing from prior test runs. Not generated by Phase 3. Suggest adding `.reports/*.png` to `.gitignore` in a future hygiene pass. |
