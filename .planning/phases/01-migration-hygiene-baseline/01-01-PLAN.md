---
phase: 01-migration-hygiene-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/archive/_deprecated_add-phone-columns.sql
  - supabase/archive/_deprecated_add-write-policies.sql
  - supabase/archive/_deprecated_child-locations.sql
  - supabase/archive/_deprecated_fix-all-rls.sql
  - supabase/archive/_deprecated_fix-rls.sql
  - supabase/archive/_deprecated_fix-rls-v2.sql
  - supabase/archive/_deprecated_fix-sync-final.sql
  - supabase/archive/_deprecated_migration.sql
  - supabase/archive/_deprecated_parent-pairing-fix.sql
  - supabase/archive/_deprecated_patch-existing-db.sql
  - supabase/archive/_deprecated_push-tables.sql
  - supabase/archive/_deprecated_stickers-and-geofence.sql
  - supabase/archive/README.md
autonomous: true
requirements: []
tags: [migration-hygiene, supabase, archive, drift-reduction]

must_haves:
  truths:
    - "Zero loose .sql files remain at supabase/ top level (only subdirs + config.toml + PUSH-SETUP.md)"
    - "Every moved file is preserved via git history as a rename (archived, not deleted)"
    - "supabase/archive/README.md declares the deprecation rationale and per-file one-line summary"
    - "A developer running `supabase db diff` no longer sees phantom drift caused by loose SQL files being read as applied schema"
  artifacts:
    - path: "supabase/archive/README.md"
      provides: "Deprecation manifest + per-file summary"
      contains: "deprecated"
    - path: "supabase/archive/_deprecated_fix-rls.sql"
      provides: "Archived copy of original fix-rls.sql (content preserved via git mv)"
    - path: "supabase/archive/_deprecated_migration.sql"
      provides: "Archived copy of original migration.sql"
  key_links:
    - from: "supabase/archive/README.md"
      to: "supabase/migrations/"
      via: "explicit pointer text"
      pattern: "supabase/migrations"
---

<objective>
Archive all 12 loose `supabase/*.sql` files into `supabase/archive/` with a `_deprecated_` prefix so they remain git-traceable, and author `supabase/archive/README.md` that declares they are no longer applied and points to `supabase/migrations/` as the canonical source.

Purpose: Remove the "two SQL directories with drift" pre-condition (PITFALLS.md line 155). Eliminates the source of `supabase db diff` noise so Plan 03's reconciliation migration can see a clean drift signal.

Output: 12 files moved under `supabase/archive/`, plus a new README. Zero behavior change — these files were never referenced by migration tooling; they were ad-hoc patches.

**File-count note (D-01 glob resolution):** CONTEXT.md D-01 lists 11 filenames but uses a `fix-rls*.sql` glob entry. The repo actually contains TWO files matching that glob — `fix-rls.sql` AND `fix-rls-v2.sql` — so the final archive count is **12, not 11**. This expansion is intentional and flows from D-17 ("executor discretion on archive sub-structure / glob expansion"). The glob resolution is documented explicitly so the Plan 01 SUMMARY is not surprising to reviewers.

Implements D-01, D-02 from CONTEXT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-migration-hygiene-baseline/01-CONTEXT.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create archive directory and move all 12 loose SQL files via `git mv`</name>
  <files>
    supabase/archive/_deprecated_add-phone-columns.sql
    supabase/archive/_deprecated_add-write-policies.sql
    supabase/archive/_deprecated_child-locations.sql
    supabase/archive/_deprecated_fix-all-rls.sql
    supabase/archive/_deprecated_fix-rls.sql
    supabase/archive/_deprecated_fix-rls-v2.sql
    supabase/archive/_deprecated_fix-sync-final.sql
    supabase/archive/_deprecated_migration.sql
    supabase/archive/_deprecated_parent-pairing-fix.sql
    supabase/archive/_deprecated_patch-existing-db.sql
    supabase/archive/_deprecated_push-tables.sql
    supabase/archive/_deprecated_stickers-and-geofence.sql
  </files>
  <read_first>
    - supabase/add-phone-columns.sql
    - supabase/add-write-policies.sql
    - supabase/child-locations.sql
    - supabase/fix-all-rls.sql
    - supabase/fix-rls.sql
    - supabase/fix-rls-v2.sql
    - supabase/fix-sync-final.sql
    - supabase/migration.sql
    - supabase/parent-pairing-fix.sql
    - supabase/patch-existing-db.sql
    - supabase/push-tables.sql
    - supabase/stickers-and-geofence.sql
    - .planning/phases/01-migration-hygiene-baseline/01-CONTEXT.md (§D-01)
  </read_first>
  <action>
    Run `mkdir -p supabase/archive` first. Then use `git mv` (NOT `mv`) for every file so history is preserved as a rename, not as a delete+add. Execute these commands verbatim from the repo root:

    ```
    mkdir -p supabase/archive
    git mv supabase/add-phone-columns.sql       supabase/archive/_deprecated_add-phone-columns.sql
    git mv supabase/add-write-policies.sql      supabase/archive/_deprecated_add-write-policies.sql
    git mv supabase/child-locations.sql         supabase/archive/_deprecated_child-locations.sql
    git mv supabase/fix-all-rls.sql             supabase/archive/_deprecated_fix-all-rls.sql
    git mv supabase/fix-rls.sql                 supabase/archive/_deprecated_fix-rls.sql
    git mv supabase/fix-rls-v2.sql              supabase/archive/_deprecated_fix-rls-v2.sql
    git mv supabase/fix-sync-final.sql          supabase/archive/_deprecated_fix-sync-final.sql
    git mv supabase/migration.sql               supabase/archive/_deprecated_migration.sql
    git mv supabase/parent-pairing-fix.sql      supabase/archive/_deprecated_parent-pairing-fix.sql
    git mv supabase/patch-existing-db.sql       supabase/archive/_deprecated_patch-existing-db.sql
    git mv supabase/push-tables.sql             supabase/archive/_deprecated_push-tables.sql
    git mv supabase/stickers-and-geofence.sql   supabase/archive/_deprecated_stickers-and-geofence.sql
    ```

    **Glob expansion explanation (D-01 → 12 files, not 11):** CONTEXT.md D-01 lists 11 filename entries, one of which is the glob `fix-rls*.sql`. The repo contains TWO matching files (`fix-rls.sql` + `fix-rls-v2.sql`), so the glob expands to 12 archived files total. D-17 (Claude's discretion on archive sub-structure) allows this resolution; a flat archive is correct — 12 files do not justify categorization.

    Do NOT edit any .sql contents during the move. Do NOT use `rm` / `rm -f`. Do NOT combine with the README commit in the same commit — the README is the next task, each is its own atomic commit.
  </action>
  <verify>
    <automated>bash -c "test -z \"$(ls supabase/*.sql 2>/dev/null)\" && [ \"$(ls supabase/archive/_deprecated_*.sql 2>/dev/null | wc -l | tr -d ' ')\" = \"12\" ] && git log --oneline -1 --name-status -- supabase/archive/ | grep -q 'R'"</automated>
  </verify>
  <acceptance_criteria>
    - `ls supabase/*.sql` returns no matches (zero loose .sql files at supabase/ top level).
    - `ls supabase/archive/_deprecated_*.sql` yields exactly 12 lines (use `wc -l | tr -d ' '` then string-compare against `12` — do NOT use `grep -q '^12$'` against raw `wc -l` output because macOS/BSD `wc` emits leading whitespace).
    - `git status` shows 12 renames (R100 or similarity ≥50%), zero deletions-without-corresponding-adds.
    - `git log --follow supabase/archive/_deprecated_fix-rls.sql` shows pre-archive history (proves rename preserved history).
    - `supabase/config.toml`, `supabase/migrations/`, `supabase/functions/`, `supabase/PUSH-SETUP.md` are UNTOUCHED.
  </acceptance_criteria>
  <done>Files moved under `supabase/archive/_deprecated_*.sql`; git recognizes them as renames; `supabase/` top level contains only subdirectories + `config.toml` + `PUSH-SETUP.md`.</done>
</task>

<task type="auto">
  <name>Task 2: Author `supabase/archive/README.md` with deprecation declaration + per-file summary</name>
  <files>supabase/archive/README.md</files>
  <read_first>
    - supabase/archive/_deprecated_add-phone-columns.sql
    - supabase/archive/_deprecated_add-write-policies.sql
    - supabase/archive/_deprecated_child-locations.sql
    - supabase/archive/_deprecated_fix-all-rls.sql
    - supabase/archive/_deprecated_fix-rls.sql
    - supabase/archive/_deprecated_fix-rls-v2.sql
    - supabase/archive/_deprecated_fix-sync-final.sql
    - supabase/archive/_deprecated_migration.sql
    - supabase/archive/_deprecated_parent-pairing-fix.sql
    - supabase/archive/_deprecated_patch-existing-db.sql
    - supabase/archive/_deprecated_push-tables.sql
    - supabase/archive/_deprecated_stickers-and-geofence.sql
    - .planning/phases/01-migration-hygiene-baseline/01-CONTEXT.md (§D-02)
  </read_first>
  <action>
    Create `supabase/archive/README.md` with the following exact structure (fill in per-file summary by reading each archived file's first 10-20 lines to describe what it did):

    ```markdown
    # supabase/archive/

    These SQL files are **DEPRECATED** and are **no longer applied by any tooling**.
    They predate migration discipline and existed as ad-hoc patches run manually
    against production before `supabase/migrations/YYYYMMDDHHMMSS_*.sql` became
    the canonical source.

    **Canonical schema source:** `supabase/migrations/`
    **Do not run these files.** They are retained for `git log` / `grep` archaeology only.

    Archived: 2026-04-21 (Phase 1 — Migration Hygiene & Baseline, per D-01/D-02).

    Note on file count: CONTEXT.md D-01 listed 11 filename entries, one being the
    `fix-rls*.sql` glob which expanded to 2 matching files (`fix-rls.sql` and
    `fix-rls-v2.sql`), so the archive contains 12 files total. Glob expansion
    handled per D-17 (executor discretion on archive layout).

    ## File summaries

    | File | One-line summary |
    |------|------------------|
    | `_deprecated_add-phone-columns.sql` | {read first 20 lines, describe: e.g., "Adds phone columns to profiles" — verify from file} |
    | `_deprecated_add-write-policies.sql` | {describe from file} |
    | `_deprecated_child-locations.sql` | {describe from file} |
    | `_deprecated_fix-all-rls.sql` | {describe from file} |
    | `_deprecated_fix-rls.sql` | {describe from file} |
    | `_deprecated_fix-rls-v2.sql` | {describe from file} |
    | `_deprecated_fix-sync-final.sql` | {describe from file} |
    | `_deprecated_migration.sql` | {describe from file} |
    | `_deprecated_parent-pairing-fix.sql` | {describe from file} |
    | `_deprecated_patch-existing-db.sql` | {describe from file} |
    | `_deprecated_push-tables.sql` | {describe from file} |
    | `_deprecated_stickers-and-geofence.sql` | {describe from file} |

    ## Why these are archived, not deleted

    1. `git log --follow` must still reach them for audit reconstruction.
    2. Phase 2 / Stream C (P0-3 pair code) may need to copy the existing
       `join_family` RPC baseline from `_deprecated_parent-pairing-fix.sql`
       into a tracked migration — per SUMMARY.md §Research Flags.
    3. Regulatory / compliance traceability — PIPA retention requires
       provenance for any SQL that touched live family data.
    ```

    For each file's one-line summary: read the file, identify the primary operation (CREATE TABLE, ALTER TABLE, CREATE POLICY, INSERT, function definition, etc.) and its target object. Write a single 8-15 word sentence. Do not copy SQL; describe intent.

    **Include the "Note on file count" paragraph verbatim** so reviewers opening the archive immediately see why 11 D-01 entries produced 12 archived files (the `fix-rls*.sql` glob resolution, per D-17 discretion).

    Do NOT include VAPID keys, service-role keys, or other secrets in the README — these archived files may reference table names but no secrets should leak through the summary.
  </action>
  <verify>
    <automated>bash -c "test -f supabase/archive/README.md && grep -q 'DEPRECATED' supabase/archive/README.md && grep -q 'supabase/migrations/' supabase/archive/README.md && grep -c '_deprecated_' supabase/archive/README.md | awk '{exit ($1 < 12)}'"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/archive/README.md` exists and is non-empty.
    - File contains the literal string `DEPRECATED` (uppercase) in the header/body.
    - File contains the literal string `supabase/migrations/` pointing to the canonical source.
    - File references all 12 archived filenames (each `_deprecated_*.sql` name appears at least once → `grep -c '_deprecated_' supabase/archive/README.md` ≥ 12).
    - File contains an archive date "2026-04-21" and a reference to D-01/D-02 for traceability.
    - File contains an explicit note explaining the 11→12 glob expansion (`fix-rls*.sql` → `fix-rls.sql` + `fix-rls-v2.sql`) under D-17 discretion.
    - File does NOT contain strings matching `VAPID.*private|service_role|SERVICE_ROLE_KEY|eyJ` (no secrets).
  </acceptance_criteria>
  <done>`supabase/archive/README.md` authored, committed; future developers opening `supabase/archive/` see the deprecation notice first and are redirected to `supabase/migrations/`.</done>
</task>

</tasks>

<verification>
After both tasks:

```
# 1. Confirm zero loose SQL at supabase/ top level
test -z "$(ls supabase/*.sql 2>/dev/null)"

# 2. Confirm 12 archived files (portable wc-output handling — macOS/BSD wc emits leading whitespace)
[ "$(ls supabase/archive/_deprecated_*.sql 2>/dev/null | wc -l | tr -d ' ')" = "12" ]

# 3. Confirm README present and references all 12 files
test -f supabase/archive/README.md
grep -c '_deprecated_' supabase/archive/README.md   # expect: ≥12

# 4. Confirm git detected renames (not delete+add)
git log --oneline -2 --name-status | grep -c '^R'   # expect: ≥12

# 5. Confirm migrations/ + functions/ + config.toml untouched
git status supabase/migrations supabase/functions supabase/config.toml   # expect: clean
```

Branch verification / Playwright: NOT in this plan's scope. The archive move is a pure filesystem rename with zero runtime behavior change — no schema, no code, no tests touched. Branch + Playwright exit gate runs end-to-end in Plan 05.
</verification>

<success_criteria>
- 12 loose `supabase/*.sql` files moved into `supabase/archive/_deprecated_*.sql` (11 D-01 entries expanded via `fix-rls*.sql` glob to 12 files, per D-17 discretion — explicitly documented in the README).
- `supabase/archive/README.md` authored with deprecation notice + per-file summaries + pointer to canonical migrations directory + glob-expansion note.
- Git history preserved (all moves registered as renames, not deletes).
- Runtime / schema / code behavior: unchanged. Zero regression risk.
- D-01 and D-02 from CONTEXT.md implemented exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-migration-hygiene-baseline/01-01-SUMMARY.md` per the template. Include a short note in the SUMMARY acknowledging the 11→12 file count (D-01 `fix-rls*.sql` glob → `fix-rls.sql` + `fix-rls-v2.sql`, 2 files) so reviewers are not surprised.
</output>
