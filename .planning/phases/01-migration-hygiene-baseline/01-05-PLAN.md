---
phase: 01-migration-hygiene-baseline
plan: 05
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03, 01-04]
files_modified:
  - .planning/phases/01-migration-hygiene-baseline/branch-apply-log.md
  - .planning/phases/01-migration-hygiene-baseline/playwright-branch-run.txt
  - .planning/phases/01-migration-hygiene-baseline/edge-function-log-watch.txt
autonomous: false
requirements: []
tags: [supabase-branch, db-push, playwright, real-services, blocking-schema-push, exit-gate]
user_setup:
  - service: supabase
    why: "Branch creation, db push, Edge Function log access require authenticated Supabase CLI"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens (non-TTY workaround for `supabase db push`)"
    dashboard_config:
      - task: "Confirm prod project is linked via `supabase link --project-ref <ref>` (one-time)"
        location: "Terminal"
      - task: "Confirm branching is enabled on the project (paid tier feature on hosted Supabase; free tier may need a manual enable)"
        location: "Supabase Dashboard → Project Settings → Branches"

must_haves:
  truths:
    - "A Supabase branch `phase-1-baseline` exists (created via `supabase branches create`)"
    - "The reconciliation migration from Plan 03 (`supabase/migrations/YYYYMMDDHHMMSS_reconcile_schema_drift.sql`) applies cleanly to that branch via `supabase db push` — NOT to main"
    - "After push, the branch's `memos` table has columns `created_at`, `user_id`, `user_role` (verifiable via `information_schema.columns`)"
    - "`npx playwright test --config=playwright.real.config.js` pointed at the branch passes GREEN across the D-11 regression flows (auth, pair, kkuk, memo, events, location)"
    - "A 5-minute Edge Function log watch against the branch shows zero new error entries for `push-notify`"
    - "Evidence of the run (apply log, Playwright output, log-watch output) is committed under `.planning/phases/01-migration-hygiene-baseline/`"
    - "The branch `phase-1-baseline` is NOT deleted by this plan — deletion is deferred until after main promotion (which is out of Phase 1 scope)"
  artifacts:
    - path: ".planning/phases/01-migration-hygiene-baseline/branch-apply-log.md"
      provides: "Chronological record of branch creation + `db push` output + column verification"
      min_lines: 20
    - path: ".planning/phases/01-migration-hygiene-baseline/playwright-branch-run.txt"
      provides: "Full stdout/stderr of the real-services Playwright run against the branch"
      min_lines: 20
    - path: ".planning/phases/01-migration-hygiene-baseline/edge-function-log-watch.txt"
      provides: "5-minute Edge Function log watch output, annotated PASS/FAIL"
      min_lines: 5
  key_links:
    - from: "supabase/migrations/YYYYMMDDHHMMSS_reconcile_schema_drift.sql"
      to: "supabase branch phase-1-baseline"
      via: "supabase db push --db-url <branch-db-url>"
      pattern: "reconcile_schema_drift"
    - from: "playwright.real.config.js"
      to: "supabase branch phase-1-baseline endpoints"
      via: "VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY set to branch values"
      pattern: "playwright.real.config.js"
---

<objective>
This is the **exit gate** for Phase 1.

Apply the reconciliation migration from Plan 03 onto a dedicated Supabase branch (NOT main), run the real-services Playwright regression suite against that branch covering the D-11 smoke flows, and observe the Edge Function logs for 5 minutes to catch runtime regressions. Capture evidence of all three for reviewer cross-check.

**Main promotion is explicitly OUT OF SCOPE for Phase 1.** No `supabase db push` hits production in this plan. The branch is left alive (NOT deleted) so a human can promote to main as a separate ops step, or Phase 2 can inherit / recreate it.

Purpose: Prove the Plan 03 migration is safe before any live-data phase (2–5) touches prod. Proves the `memos` 42703 error is gone on the branch. Establishes the Supabase branch workflow discipline declared in ARCHITECTURE.md §3.1 — every future phase repeats this pattern.

Output:
- New Supabase branch `phase-1-baseline` (remote, on Supabase hosted infra).
- Reconciliation migration APPLIED on the branch.
- Playwright real-services suite GREEN against the branch.
- 5-min Edge Function log watch clean.
- Three evidence files committed under `.planning/phases/01-migration-hygiene-baseline/`.

Implements D-11, D-12, D-13, D-14 from CONTEXT.md.

**This plan contains the [BLOCKING] schema push.** Until `supabase db push` on the branch succeeds, the phase cannot close.

**Two checkpoints:**
1. Before the migration is pushed, the user reviews the exact `supabase db push --dry-run` output to confirm only the expected DDL applies.
2. Before the branch is left alive (end of plan), the user confirms Playwright is green and Edge Function logs are clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-migration-hygiene-baseline/01-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-migration-hygiene-baseline/01-01-SUMMARY.md
@.planning/phases/01-migration-hygiene-baseline/01-02-SUMMARY.md
@.planning/phases/01-migration-hygiene-baseline/01-03-SUMMARY.md
@.planning/phases/01-migration-hygiene-baseline/01-04-SUMMARY.md
@playwright.real.config.js
@tests/e2e/family-journey-real.spec.js
@tests/e2e/kakao-real.spec.js
@tests/e2e/realtime-connection-real.spec.js
</context>

<interfaces>
<!-- What the branch apply step must produce, contract-style -->

**Supabase CLI contract (v1.200+):**
- `supabase branches create <name>` → creates isolated Postgres + anon/service keys on Supabase hosted infra; copies `supabase/migrations/*.sql` schema automatically.
- `supabase branches get <name> --output json` → returns branch metadata including `db_host`, `db_name`, `api_url`, `anon_key`, `service_role_key`.
- `supabase db push --db-url <url> --dry-run` → previews DDL without applying.
- `supabase db push --db-url <url>` → applies all unapplied migrations in order.
- `supabase branches delete <name>` → tears down the branch (NOT called by this plan).
- Non-TTY: set `SUPABASE_ACCESS_TOKEN` env var; CLI will not prompt for login.

**Playwright env contract (from tests/e2e/family-journey-real.spec.js lines 16-17):**
- `VITE_SUPABASE_URL` — branch API URL (from `supabase branches get --output json`)
- `VITE_SUPABASE_ANON_KEY` — branch anon JWT (from same)
- Read from `.env` at playwright.real.config.js startup; override per-run via shell env.

**D-11 smoke flow coverage (seven flows):**
1. 역할 선택(부모) → Kakao OAuth → `families` row + `pair_code`
2. 역할 선택(아이) → 익명 로그인 → 잘못된 코드 rejection
3. 아이 페어링 → `family_members` row
4. 부모 → 아이 꾹 → realtime overlay
5. 부모 메모 → `memo_replies` INSERT → 아이 UI
6. 아이 메모 답글 → `memo_replies` INSERT → 부모 UI
7. 부모 이벤트 → 아이 polling UI

The existing specs in `tests/e2e/*-real.spec.js` already cover ≥5 of the 7 (via `family-journey-real.spec.js`, `realtime-connection-real.spec.js`, `kakao-real.spec.js`, `parent-real.spec.js`, `child-anon-real.spec.js`). D-12 says "preserve existing specs; add only missing flows."

**Out of scope here:**
- Adding NEW Playwright specs for Phase 1 (no new REQs exist in Phase 1; existing specs cover the regression surface).
- Promoting the migration to main (deferred).
- Deleting the branch (deferred per D-14).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Supabase branch `phase-1-baseline` and capture its connection details</name>
  <files>
    .planning/phases/01-migration-hygiene-baseline/branch-apply-log.md
  </files>
  <read_first>
    - .planning/phases/01-migration-hygiene-baseline/01-CONTEXT.md (§D-13)
    - .planning/research/ARCHITECTURE.md §3.1 (7-step workflow)
    - supabase/config.toml (to confirm project ref is linkable)
  </read_first>
  <action>
    **Prerequisites (executor must verify before running commands):**
    - `SUPABASE_ACCESS_TOKEN` is set (verify with `supabase projects list` — must succeed without password prompt).
    - The prod project is already linked (`ls supabase/.temp/project-ref` or `supabase projects list --output pretty` shows a "Linked" marker).
    - Branching feature is enabled on the project (paid-tier feature on hosted Supabase; if the `supabase branches create` command returns "branching not enabled," the executor STOPS and surfaces the issue to the user — this is a D-13 blocker requiring dashboard enable or plan upgrade, not something the executor can work around).

    **Create the branch and capture metadata:**
    ```
    # 1. Create branch. Name must match D-13 exactly: phase-1-baseline.
    supabase branches create phase-1-baseline 2>&1 | tee /tmp/branch-create.log

    # 2. Fetch connection metadata. Redirect json to a local file we can parse.
    supabase branches get phase-1-baseline --output json > /tmp/branch-meta.json 2>&1

    # 3. Validate the branch came up healthy — `db_host`, `api_url`, `anon_key` all non-empty.
    jq -e '.db_host and .api_url and .anon_key' /tmp/branch-meta.json
    ```

    Then author the evidence file `.planning/phases/01-migration-hygiene-baseline/branch-apply-log.md`, appending a section like:

    ```markdown
    # Phase 1 — Branch Apply Log

    Started: <UTC timestamp>
    Branch: phase-1-baseline
    Target migration: <filename from Plan 03, e.g., 20260421153000_reconcile_schema_drift.sql>

    ## Step 1 — Branch creation

    Command: `supabase branches create phase-1-baseline`
    Result: <paste output, excluding any keys; redact JWTs and keys>
    Branch ID: <from meta, public>
    API URL: <from meta, public — no auth tokens here>

    ## Step 2 — Connection metadata sanity check

    - `db_host`: <redacted-host>.supabase.co   # host only, no password
    - `api_url`: https://<branch-ref>.supabase.co
    - `anon_key`: eyJhbGci... (first 16 chars only; full value lives in shell env, not in git)
    - `service_role_key`: [REDACTED — never committed]

    Status: BRANCH HEALTHY ✓
    ```

    **Hard rules:**
    - Do NOT commit the raw `branch-meta.json` file — it contains `service_role_key` and `anon_key` values. Only commit the redacted markdown summary.
    - Do NOT export the service_role key to any location other than the current shell session.
    - Do NOT run the branch in yolo mode — if `supabase branches create` errors, STOP and surface to user; do not retry-with-force.

    Record the following values in SHELL ENV (export for subsequent tasks in this plan — they live in the executor's shell, not in git):
    ```
    export BRANCH_API_URL="$(jq -r '.api_url' /tmp/branch-meta.json)"
    export BRANCH_ANON_KEY="$(jq -r '.anon_key' /tmp/branch-meta.json)"
    export BRANCH_DB_URL="$(jq -r '.postgres_db_url // .db_url' /tmp/branch-meta.json)"
    # Check: all three are non-empty.
    [ -n "$BRANCH_API_URL" ] && [ -n "$BRANCH_ANON_KEY" ] && [ -n "$BRANCH_DB_URL" ] || { echo "missing metadata"; exit 1; }
    ```
  </action>
  <verify>
    <automated>bash -c "test -f .planning/phases/01-migration-hygiene-baseline/branch-apply-log.md && grep -q 'phase-1-baseline' .planning/phases/01-migration-hygiene-baseline/branch-apply-log.md && grep -q 'BRANCH HEALTHY' .planning/phases/01-migration-hygiene-baseline/branch-apply-log.md && ! grep -qE 'service_role_key\"? *: *\"eyJ' .planning/phases/01-migration-hygiene-baseline/branch-apply-log.md"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase branches get phase-1-baseline` returns a healthy branch (jq validation passes).
    - `.planning/phases/01-migration-hygiene-baseline/branch-apply-log.md` exists, contains `phase-1-baseline` and `BRANCH HEALTHY` markers.
    - The log file does NOT contain a full `service_role_key` value (`grep -E 'service_role.*eyJ[A-Za-z0-9_-]{40,}'` returns nothing).
    - Shell env `BRANCH_API_URL`, `BRANCH_ANON_KEY`, `BRANCH_DB_URL` are populated (confirmed inline by the commands in `<action>`).
    - No changes to `main` branch on Supabase. No migrations applied yet — this step only provisions the branch.
  </acceptance_criteria>
  <done>Branch `phase-1-baseline` provisioned on Supabase hosted infra; executor holds connection metadata in shell env for Tasks 2-3; redacted evidence log committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human reviews `supabase db push --dry-run` output before the BLOCKING schema push</name>
  <what-built>
    - Supabase branch `phase-1-baseline` provisioned (Task 1).
    - A dry-run of the reconciliation migration will be shown below.
  </what-built>
  <how-to-verify>
    The executor runs a DRY-RUN of the migration push against the branch and displays the exact DDL that will apply. The user reviews it BEFORE authorizing the real push.

    Executor runs (non-destructive; no DDL applied):
    ```
    supabase db push --db-url "$BRANCH_DB_URL" --dry-run 2>&1 | tee /tmp/db-push-dryrun.log
    ```

    The dry-run output should show exactly the reconciliation migration from Plan 03 as the only unapplied migration (because branch creation copies all prior migrations already). Expected output pattern:

    ```
    Applying migration YYYYMMDDHHMMSS_reconcile_schema_drift.sql...
    (DRY RUN — no changes applied)
    ```

    **User verification steps (run yourself, confirm each):**

    1. Open `/tmp/db-push-dryrun.log` and confirm the only pending migration is the `_reconcile_schema_drift.sql` file authored in Plan 03.

    2. Confirm the dry-run shows these three `ALTER TABLE public.memos ADD COLUMN IF NOT EXISTS` statements:
       - `created_at timestamptz DEFAULT now()`
       - `user_id uuid REFERENCES auth.users(id) ON DELETE SET NULL`
       - `user_role text`

    3. Confirm NO `DROP TABLE`, `DROP POLICY`, `TRUNCATE`, or `DELETE FROM` statements are present in the dry-run output.

    4. If Plan 03's Task 2 absorbed additional drift items (beyond the three `memos` columns), confirm each one is expected and has a matching entry in `.planning/phases/01-migration-hygiene-baseline/db-diff-output.txt` from Plan 03.

    5. Confirm the dry-run ran against `$BRANCH_DB_URL`, not against the prod connection string. (Inspect the first few log lines for the host — it should contain the branch project ref, not the production project ref.)

    If all five checks pass → type "approved" → executor proceeds to Task 3 (real push).

    If anything is unexpected → type "abort" → executor STOPS, does not apply the migration, surfaces the finding to the user.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the real `supabase db push` against the branch, or "abort" + description of concern to stop.</resume-signal>
  <acceptance_criteria>
    - User has reviewed `/tmp/db-push-dryrun.log`.
    - The only pending migration is the reconciliation file from Plan 03.
    - No destructive DDL in the dry-run output.
    - Dry-run target host matches the branch, not prod.
    - User explicitly types "approved" to continue.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Apply the reconciliation migration to the branch via `supabase db push`</name>
  <files>
    .planning/phases/01-migration-hygiene-baseline/branch-apply-log.md
  </files>
  <read_first>
    - .planning/phases/01-migration-hygiene-baseline/branch-apply-log.md (from Task 1 — append to it)
    - .planning/phases/01-migration-hygiene-baseline/01-CONTEXT.md (§D-13)
    - /tmp/db-push-dryrun.log (from the checkpoint — verify it matches what we're about to apply)
  </read_first>
  <action>
    **[BLOCKING] Schema Push — mandatory per planner requirement.** This task is the one that cannot be skipped; the phase cannot close without confirming the reconciliation migration applied cleanly on the branch.

    Confirm env vars from Task 1 still populated (`[ -n "$BRANCH_DB_URL" ]`). If the shell session was reset between tasks, re-export them from `supabase branches get phase-1-baseline --output json`.

    Apply the migration against THE BRANCH — NOT main:

    ```
    # CRITICAL: --db-url flag must be the branch URL, never prod.
    supabase db push --db-url "$BRANCH_DB_URL" 2>&1 | tee /tmp/db-push-apply.log
    ```

    If the push exits non-zero:
    - Capture the error in the branch-apply-log.md.
    - Do NOT retry without reading the error. Common failure modes:
      - Missing `SUPABASE_ACCESS_TOKEN` → re-export and retry once.
      - Migration SQL syntax error → back to Plan 03; do NOT patch-in-place here.
      - Partial apply (BEGIN failed mid-transaction) → branch state is still the pre-apply state because of the BEGIN/COMMIT wrap; investigate root cause.
    - If the push fails twice with distinct errors, STOP and surface to the user. Do NOT push to main under any circumstance.

    **Post-push verification — the migration actually applied:**
    ```
    # Verify `memos` now has the three new columns on the branch.
    psql "$BRANCH_DB_URL" -c "\
      SELECT column_name, data_type, is_nullable \
      FROM information_schema.columns \
      WHERE table_schema='public' AND table_name='memos' \
        AND column_name IN ('created_at','user_id','user_role') \
      ORDER BY column_name;" 2>&1 | tee /tmp/memos-columns-after.log
    ```

    Expected output (exact ordering, all three present, ALL THREE nullable per CONTEXT.md D-06):
    ```
     column_name | data_type                   | is_nullable
    -------------+-----------------------------+-------------
     created_at  | timestamp with time zone    | YES         # nullable per D-06 (DEFAULT now() for new rows, NULL for legacy)
     user_id     | uuid                        | YES         # nullable per D-06
     user_role   | text                        | YES         # nullable per D-06
    ```

    Append to `branch-apply-log.md`:
    ```markdown
    ## Step 3 — BLOCKING schema push applied

    Command: `supabase db push --db-url <BRANCH_DB_URL>`   # target: phase-1-baseline ONLY, never prod
    Executed at: <UTC timestamp>
    Exit code: 0
    Applied migration: <YYYYMMDDHHMMSS>_reconcile_schema_drift.sql

    ### Post-apply column verification (information_schema.columns)

    | column_name | data_type                   | is_nullable |
    |-------------|-----------------------------|-------------|
    | created_at  | timestamp with time zone    | YES         |
    | user_id     | uuid                        | YES         |
    | user_role   | text                        | YES         |

    **Status: BLOCKING TASK SATISFIED ✓**
    ```

    **Hard rules:**
    - The `--db-url` value MUST be the branch URL. Before running, print the first 40 characters of `$BRANCH_DB_URL` and confirm it contains the branch project ref, not the production ref. (Executor MUST do this check inline; it is a "catch the typo that kills production" safeguard.)
    - Do NOT run `supabase db push` with no `--db-url` flag — the default behavior pushes to the linked project (prod). The flag is mandatory.
    - Do NOT apply the migration to main in this plan. D-14's branch deletion AND the promotion to main are both deferred to a later step.
    - If the post-apply column check shows `is_nullable = NO` on ANY of `created_at`, `user_id`, or `user_role`, the migration has a bug — STOP, do not continue to Playwright, back to Plan 03 to fix. All three columns MUST be nullable per CONTEXT.md D-06.
  </action>
  <verify>
    <automated>bash -c "test -f /tmp/db-push-apply.log && grep -q 'reconcile_schema_drift' /tmp/db-push-apply.log && test -f /tmp/memos-columns-after.log && grep -q 'created_at' /tmp/memos-columns-after.log && grep -q 'user_id' /tmp/memos-columns-after.log && grep -q 'user_role' /tmp/memos-columns-after.log && grep -q 'BLOCKING TASK SATISFIED' .planning/phases/01-migration-hygiene-baseline/branch-apply-log.md"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push --db-url "$BRANCH_DB_URL"` exited with code 0.
    - `/tmp/db-push-apply.log` references the reconciliation migration filename.
    - `information_schema.columns` on the branch now shows all three new `memos` columns: `created_at` (nullable), `user_id` (nullable), `user_role` (nullable). All three MUST be `is_nullable = YES` per CONTEXT.md D-06.
    - `branch-apply-log.md` contains the literal marker `BLOCKING TASK SATISFIED`.
    - Production database (`main` Supabase project) has NOT been touched. A `psql "$PROD_READ_ONLY_DATABASE_URL" -c "SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='memos' AND column_name='created_at'"` against prod should either show the column ALREADY present (if it was the original drift) or NOT present (if the drift was different) — but crucially, the state of prod MUST be unchanged from before this plan started. (Executor confirms by comparing against `.planning/phases/01-migration-hygiene-baseline/db-diff-output.txt` from Plan 03.)
  </acceptance_criteria>
  <done>[BLOCKING] Reconciliation migration applied on the `phase-1-baseline` branch; three new `memos` columns verified present on the branch; prod untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Run Playwright real-services suite against the branch; capture full output</name>
  <files>
    .planning/phases/01-migration-hygiene-baseline/playwright-branch-run.txt
  </files>
  <read_first>
    - playwright.real.config.js (env var contract: VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY)
    - tests/e2e/family-journey-real.spec.js (existing parent+child happy path — covers flows 1-6 of D-11)
    - tests/e2e/realtime-connection-real.spec.js (realtime coverage)
    - tests/e2e/kakao-real.spec.js, tests/e2e/parent-real.spec.js, tests/e2e/child-anon-real.spec.js
    - .planning/phases/01-migration-hygiene-baseline/01-CONTEXT.md (§D-11, §D-12)
  </read_first>
  <action>
    Run the real-services Playwright suite with `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` overridden to the branch values.

    ```
    # Sanity check: shell env from Task 1 still populated.
    [ -n "$BRANCH_API_URL" ] && [ -n "$BRANCH_ANON_KEY" ] || { \
      echo "Re-fetching branch metadata..."; \
      meta=$(supabase branches get phase-1-baseline --output json); \
      export BRANCH_API_URL=$(echo "$meta" | jq -r '.api_url'); \
      export BRANCH_ANON_KEY=$(echo "$meta" | jq -r '.anon_key'); \
    }

    # Run the full real-services suite against the branch.
    # Note: playwright.real.config.js auto-loads .env — but shell env takes
    # precedence (`if !(key in process.env) { process.env[key] = value }`
    # at playwright.real.config.js line 19-21).
    VITE_SUPABASE_URL="$BRANCH_API_URL" \
    VITE_SUPABASE_ANON_KEY="$BRANCH_ANON_KEY" \
      npx playwright test --config=playwright.real.config.js \
        --reporter=list \
        2>&1 | tee /tmp/playwright-branch.log

    # Capture exit code (tee masks it).
    PW_EXIT="${PIPESTATUS[0]}"
    echo "Playwright exit code: $PW_EXIT" | tee -a /tmp/playwright-branch.log
    ```

    Then copy the log into the planning dir (redact any Supabase JWTs that leaked into error traces):

    ```
    # Redact any stray JWTs (defensive — Playwright may log them on failure).
    sed -E 's/eyJ[A-Za-z0-9_-]{20,}\.[A-Za-z0-9_-]{20,}\.[A-Za-z0-9_-]{20,}/[REDACTED_JWT]/g' \
      /tmp/playwright-branch.log \
      > .planning/phases/01-migration-hygiene-baseline/playwright-branch-run.txt
    ```

    **D-11 regression coverage check.** After the run, confirm the seven flows from D-11 are covered by the specs that ran. The existing specs already cover:
    - Flow 1 (parent role + pair_code creation) → `family-journey-real.spec.js` + `kakao-real.spec.js` / `parent-real.spec.js`
    - Flow 2 (child anon + wrong code) → `child-anon-real.spec.js`
    - Flow 3 (child pairing) → `family-journey-real.spec.js`
    - Flow 4 (parent kkuk → overlay) → `realtime-connection-real.spec.js`
    - Flow 5, 6 (memo round trip) → `family-journey-real.spec.js` (memo_replies coverage)
    - Flow 7 (event creation + polling) → `family-journey-real.spec.js` / `parent-real.spec.js`

    D-12 says "preserve existing specs + add the missing flows only." After inspection, if a flow is not covered, the executor notes which one in the log but does NOT add a new spec in this plan (new-spec authorship is not a Phase 1 deliverable — Phase 1 validates regression only; the existing 11 real-services specs are the canary set).

    **Failure handling:**
    - If ANY spec fails, do NOT continue to Task 5. Examine the failure in the captured log:
      - If the failure is a pre-existing flake (same test fails against prod too), document as known-bad; not a Phase 1 gate.
      - If the failure is new (introduced by the migration), STOP and return to Plan 03 — the migration has a regression.
    - If ≥1 test fails NEW (not pre-existing), mark the plan status as FAILED and surface to user. Do not proceed to close Phase 1.
    - If all tests pass OR only pre-existing flakes fail (documented), mark Playwright as GREEN and proceed.

    Handle Playwright flakes pragmatically: `playwright.real.config.js` sets `retries: 1`, so a one-time transient failure auto-retries. Only if a spec fails BOTH attempts is it a real failure.
  </action>
  <verify>
    <automated>bash -c "test -f .planning/phases/01-migration-hygiene-baseline/playwright-branch-run.txt && test -s .planning/phases/01-migration-hygiene-baseline/playwright-branch-run.txt && grep -qE 'passed|failed' .planning/phases/01-migration-hygiene-baseline/playwright-branch-run.txt && grep -q 'Playwright exit code:' .planning/phases/01-migration-hygiene-baseline/playwright-branch-run.txt && ! grep -qE 'eyJ[A-Za-z0-9_-]{80,}' .planning/phases/01-migration-hygiene-baseline/playwright-branch-run.txt"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-migration-hygiene-baseline/playwright-branch-run.txt` exists, non-empty, and includes both a summary line (`N passed` / `N failed`) and a final `Playwright exit code:` line.
    - Exit code is 0 (or exit code is non-zero but the failures are documented as pre-existing flakes, with reasoning recorded in `branch-apply-log.md` Step 4).
    - D-11 coverage noted: the seven smoke flows are traced to specific specs in the log OR the executor documents which specific flow is not covered by any existing spec (permitted — Phase 1 does not add new coverage).
    - No long JWTs remain in the committed log (`grep -qE 'eyJ[A-Za-z0-9_-]{80,}'` returns nothing — `eyJ` prefixes are fine up to 80 chars for short JWT header fragments; full tokens must be redacted).
    - The suite ran against the branch URL (`$BRANCH_API_URL` in Playwright output, NOT the prod URL).
  </acceptance_criteria>
  <done>Playwright real-services suite ran against the `phase-1-baseline` branch; full output captured, redacted of secrets, and committed under the planning dir; D-11 regression evidence collected.</done>
</task>

<task type="auto">
  <name>Task 5: Watch Edge Function logs on the branch for 5 minutes; capture output</name>
  <files>
    .planning/phases/01-migration-hygiene-baseline/edge-function-log-watch.txt
  </files>
  <read_first>
    - .planning/phases/01-migration-hygiene-baseline/playwright-branch-run.txt (from Task 4 — confirm suite passed before starting the watch)
    - .planning/phases/01-migration-hygiene-baseline/01-CONTEXT.md (§D-13 — "5-minute smoke with Edge Function log watch")
  </read_first>
  <action>
    Tail the Supabase Edge Function logs for the `phase-1-baseline` branch's `push-notify` function for 5 minutes while a second shell re-runs one or two Playwright specs to generate live push traffic. The goal: observe the function handling real traffic and confirm zero new errors.

    ```
    # Shell A (foreground) — follow logs for push-notify on the branch for 5 min.
    # The --project-ref flag targets the BRANCH's project ref, not prod.
    BRANCH_REF=$(supabase branches get phase-1-baseline --output json | jq -r '.project_ref')
    [ -n "$BRANCH_REF" ] || { echo "missing branch project_ref"; exit 1; }

    timeout 300 supabase functions logs push-notify \
      --project-ref "$BRANCH_REF" \
      --follow \
      2>&1 | tee /tmp/edge-log-watch.log &
    LOG_PID=$!

    # Wait briefly, then in the same shell trigger a small subset of real-services
    # specs to generate traffic against the branch's push-notify function.
    sleep 10

    # Shell B (same shell, backgrounded) — trigger traffic.
    VITE_SUPABASE_URL="$BRANCH_API_URL" \
    VITE_SUPABASE_ANON_KEY="$BRANCH_ANON_KEY" \
      npx playwright test --config=playwright.real.config.js \
        --grep "realtime|kkuk" \
        --reporter=list \
        >> /tmp/edge-log-watch.log 2>&1

    # Let the log follow continue for the remaining ~4 minutes.
    wait $LOG_PID || true    # timeout returns 124 when hit; treat as expected

    # Copy + redact.
    sed -E 's/eyJ[A-Za-z0-9_-]{20,}\.[A-Za-z0-9_-]{20,}\.[A-Za-z0-9_-]{20,}/[REDACTED_JWT]/g' \
      /tmp/edge-log-watch.log \
      > .planning/phases/01-migration-hygiene-baseline/edge-function-log-watch.txt
    ```

    **Interpret the output:**
    - PASS: Zero log entries with severity `error` or `fatal`; status codes observed are 2xx (accepting that `push-notify` itself may return 401 due to the PUSH-01 bug Phase 2 Stream A will fix — a KNOWN PRE-EXISTING state, not a regression caused by this plan).
    - PASS: Observed 401s that match the PUSH-01 known-bad state should be annotated: "Observed PUSH-01 401s during log watch — EXPECTED (pre-existing, not a Phase 1 regression). Phase 2 Stream A will fix."
    - FAIL: Any NEW error type not seen in the baseline state. Examples: 500s, unhandled exceptions, `TypeError`, `ReferenceError`, `supabase-js` crashes.

    Append to `branch-apply-log.md` (Step 5):
    ```markdown
    ## Step 5 — Edge Function 5-minute log watch

    Duration: 5 minutes
    Function observed: push-notify
    Traffic generator: `npx playwright test --grep "realtime|kkuk"`
    New errors introduced by Phase 1: 0
    Pre-existing PUSH-01 401s observed: <count>   # expected, Phase 2 fixes

    Status: LOG WATCH CLEAN ✓
    ```

    **Hard rules:**
    - Do NOT log-watch the prod project_ref. `--project-ref` must be the branch ref.
    - Do NOT over-interpret 401 / "JWT invalid" as a new failure — PUSH-01 is a KNOWN pre-existing state that Phase 2 Stream A addresses. Confirm any 401 seen matches the PUSH-01 signature (`jwt verify failed` / `ES256` token parse error) before flagging as pre-existing.
    - If `supabase functions logs` fails entirely (e.g., CLI version does not support `--follow` on branches), fall back to the Supabase Dashboard UI: user navigates to Dashboard → Functions → push-notify → Logs, filters to the last 5 minutes, copies the content into `edge-function-log-watch.txt`. Document the fallback path in `branch-apply-log.md`.
  </action>
  <verify>
    <automated>bash -c "test -f .planning/phases/01-migration-hygiene-baseline/edge-function-log-watch.txt && test -s .planning/phases/01-migration-hygiene-baseline/edge-function-log-watch.txt && grep -q 'LOG WATCH CLEAN' .planning/phases/01-migration-hygiene-baseline/branch-apply-log.md && ! grep -qiE 'TypeError|ReferenceError|uncaught' .planning/phases/01-migration-hygiene-baseline/edge-function-log-watch.txt"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-migration-hygiene-baseline/edge-function-log-watch.txt` exists, non-empty.
    - File does NOT contain `TypeError`, `ReferenceError`, or `uncaught` — those indicate new runtime regressions.
    - File may contain 401s matching PUSH-01 signature; those are annotated as pre-existing in `branch-apply-log.md`.
    - `branch-apply-log.md` Step 5 records the literal marker `LOG WATCH CLEAN ✓`.
    - No JWTs in the committed file (redaction applied).
  </acceptance_criteria>
  <done>5-minute Edge Function log watch complete; no NEW errors observed; evidence captured; phase exit gate for Edge Function stability satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human confirms Playwright GREEN + log watch clean before Phase 1 closes</name>
  <what-built>
    - Branch `phase-1-baseline` alive on Supabase hosted infra.
    - Reconciliation migration applied on the branch (three new `memos` columns present).
    - Playwright real-services suite ran against the branch.
    - 5-minute Edge Function log watch captured.
  </what-built>
  <how-to-verify>
    Final eye-check before the phase closes. Review these three evidence files:

    1. `.planning/phases/01-migration-hygiene-baseline/branch-apply-log.md`
       - Confirm `BRANCH HEALTHY`, `BLOCKING TASK SATISFIED`, and `LOG WATCH CLEAN` all present.
       - Confirm `supabase db push` was against `phase-1-baseline`, never prod.

    2. `.planning/phases/01-migration-hygiene-baseline/playwright-branch-run.txt`
       - Confirm the summary line says `N passed` with 0 new failures.
       - If any failures exist, confirm they are documented as pre-existing flakes in `branch-apply-log.md`.
       - Confirm the run targeted the branch URL, not prod.

    3. `.planning/phases/01-migration-hygiene-baseline/edge-function-log-watch.txt`
       - Scan for `TypeError`, `ReferenceError`, `uncaught`, `500`, `fatal` — zero matches expected (PUSH-01 401s are fine, pre-existing).

    4. Sanity check: `git status` shows only the three evidence files (and any SUMMARY.md from prior plans) as changed. No stray code or test changes.

    5. Confirm the branch `phase-1-baseline` is still alive (`supabase branches get phase-1-baseline --output json | jq -r '.status'` → expect `ACTIVE_HEALTHY` or similar). Per D-14, the branch MUST NOT be deleted by this plan — a later ops step promotes to main and then deletes.

    If all five checks pass → type "approved" → Phase 1 closes.

    If anything is off → type "reopen" + description → stay in the phase, fix, re-run whichever task failed.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 1, or "reopen" + description to return to the failing task.</resume-signal>
  <acceptance_criteria>
    - All three evidence files present and readable.
    - No NEW errors / failures introduced by this phase.
    - Branch `phase-1-baseline` alive (not deleted).
    - Prod database state matches its pre-phase snapshot (no accidental promotion).
    - User confirms "approved" explicitly.
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all five tasks:

```
# 1. All three evidence files present and non-empty
for f in branch-apply-log.md playwright-branch-run.txt edge-function-log-watch.txt; do
  test -s ".planning/phases/01-migration-hygiene-baseline/$f" || { echo "MISSING: $f"; exit 1; }
done

# 2. BLOCKING task marker present
grep -q 'BLOCKING TASK SATISFIED' .planning/phases/01-migration-hygiene-baseline/branch-apply-log.md

# 3. Branch reconciliation verified
grep -q 'created_at' .planning/phases/01-migration-hygiene-baseline/branch-apply-log.md
grep -q 'user_id'    .planning/phases/01-migration-hygiene-baseline/branch-apply-log.md
grep -q 'user_role'  .planning/phases/01-migration-hygiene-baseline/branch-apply-log.md

# 4. Playwright ran and produced a result line
grep -qE 'passed|failed' .planning/phases/01-migration-hygiene-baseline/playwright-branch-run.txt

# 5. Edge Function watch clean (no NEW errors)
! grep -qiE 'TypeError|ReferenceError|uncaught' .planning/phases/01-migration-hygiene-baseline/edge-function-log-watch.txt

# 6. Log files have no leaked JWTs
! grep -qE 'eyJ[A-Za-z0-9_-]{80,}\.[A-Za-z0-9_-]{30,}\.[A-Za-z0-9_-]{30,}' .planning/phases/01-migration-hygiene-baseline/playwright-branch-run.txt
! grep -qE 'eyJ[A-Za-z0-9_-]{80,}\.[A-Za-z0-9_-]{30,}\.[A-Za-z0-9_-]{30,}' .planning/phases/01-migration-hygiene-baseline/edge-function-log-watch.txt

# 7. Branch is alive (NOT deleted) — D-14
supabase branches get phase-1-baseline --output json | jq -e '.status'
```
</verification>

<success_criteria>
- Supabase branch `phase-1-baseline` created via `supabase branches create`.
- Reconciliation migration from Plan 03 applied on the branch via `supabase db push --db-url <branch-url>` — the **[BLOCKING] schema push** task.
- Column verification on the branch confirms `memos.created_at`, `memos.user_id`, `memos.user_role` all present with correct nullability.
- Playwright real-services suite (`npx playwright test --config=playwright.real.config.js`) ran against the branch and shows zero NEW failures compared to prod baseline.
- 5-minute Edge Function log watch on `push-notify` shows zero NEW errors (pre-existing PUSH-01 401s annotated as expected).
- Production database UNTOUCHED — no `supabase db push` ever hit main in this plan.
- Branch NOT deleted (per D-14 — promotion + deletion are deferred out of Phase 1).
- D-11, D-12, D-13, D-14 from CONTEXT.md implemented.
- Phase 1 ROADMAP success criteria #4 satisfied: "Playwright real-services smoke proves no regression on existing flows."
</success_criteria>

<output>
After completion, create `.planning/phases/01-migration-hygiene-baseline/01-05-SUMMARY.md` per the template, including:
- Branch name: `phase-1-baseline`
- Branch project_ref (public identifier, OK to commit)
- Exact migration filename applied
- Playwright pass/fail counts (e.g., "23 passed, 0 failed across 3 browsers")
- Edge Function log watch summary ("0 new errors; N pre-existing PUSH-01 401s observed, expected per SUMMARY.md §Executive Summary #1")
- Explicit note: "Branch `phase-1-baseline` LEFT ALIVE per D-14; promotion to main and branch deletion are out of Phase 1 scope."
- Ready-for-Phase-2 confirmation.
</output>
