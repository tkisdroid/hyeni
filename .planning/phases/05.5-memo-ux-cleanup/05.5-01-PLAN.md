---
phase: 05.5-memo-ux-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.jsx
autonomous: true
requirements:
  - MEMO-FIX-01
  - MEMO-FIX-02
  - MEMO-FIX-03
  - MEMO-FIX-05
tags:
  - memo
  - ui
  - chat-bubbles
  - legacy-removal

must_haves:
  truths:
    - "송신자가 메시지를 보내면 본인 화면에 즉시 말풍선으로 나타난다 (optimistic insert, 기존 memo_replies 경로 유지)"
    - "수신자 화면에서 상대 메시지가 Realtime 으로 3초 이내에 말풍선으로 나타난다"
    - "textarea(상단 Daily memo body 블록)가 화면에서 완전히 사라진다 — 'ghost cursor' 증상 소멸"
    - "같은 발신자가 3분 이내 보낸 연속 메시지는 아바타 1회 + 마지막 bubble 하단 timestamp 1회로 grouping 된다"
    - "날짜가 바뀌는 지점에 '오늘 / 어제 / 4월 20일 월요일' 형태의 date separator pill 이 렌더링된다"
    - "replies.length === 0 인 경우 empty state (💗 + 역할별 문구) 가 렌더링된다"
    - "sendMemo() 가 reject 되면 role=alert · aria-live=assertive 토스트가 뜨고 '다시 시도' 버튼이 제공된다"
    - "최초 MemoSection 진입 시 localStorage.memoOnboardingV2Seen 이 없으면 브랜드 핑크 온보딩 토스트가 뜨고, 두 번째 방문부터는 뜨지 않는다"
    - "grep -rn \"upsertMemo\" src/ 결과가 0건 (함수 정의는 sync.js 에 남아도 App.jsx 내 호출처 0)"
    - "prefers-reduced-motion: reduce 환경에서는 bubbleIn / toastIn 애니메이션이 발동하지 않는다"
  artifacts:
    - path: "src/App.jsx"
      provides: "MemoSection 재구성 (textarea 제거, bubble grouping, date separator, 2 toasts) 및 onMemoSend/onMemoBlur/upsertMemo 호출 제거"
      min_lines: 6877  # 기존 라인 수를 깨뜨리지 않는 monolith 보존 (±50 라인 범위 내 순증감 허용)
      contains:
        - "memoOnboardingV2Seen"
        - "bubbleIn"
        - "role=\"log\""
        - "role=\"alert\""
        - "#FEE2E2"
        - "#E879A0"
  key_links:
    - from: "src/App.jsx MemoSection (around line 2059-2198)"
      to: "replies array grouping + render"
      via: "same-sender + 180s window + date-key 비교"
      pattern: "r\\.user_id === prev\\.user_id|created_at|180_?000|3 \\* 60 \\* 1000"
    - from: "src/App.jsx MemoSection composer"
      to: "onReplySubmit (App.jsx:6789-6811)"
      via: "handleSend → onReplySubmit(text) — 기존 경로 불변"
      pattern: "onReplySubmit\\(.*text.*\\)"
    - from: "src/App.jsx onReplySubmit error path"
      to: "send-failure toast state"
      via: "sendMemo() .catch() → setShowSendFailureToast(true) 또는 동등 mechanism"
      pattern: "setShowSendFailureToast|send_?failure"
    - from: "src/App.jsx MemoSection mount"
      to: "localStorage.memoOnboardingV2Seen"
      via: "useEffect on mount → show toast if absent → set flag on first render"
      pattern: "memoOnboardingV2Seen"
    - from: "src/App.jsx onMemosChange (line 4664-4678)"
      to: "memos state setter"
      via: "narrowed skip: own-user row only OR full removal"
      pattern: "onMemosChange.*dateKeyRef|authUser\\?\\.id"
---

<objective>
Phase 5.5 의 UI 표면 수술을 단일 atomic commit 으로 완료한다. legacy `public.memos` textarea 쓰기 경로를 App.jsx 에서 제거하고, 05.5-UI-SPEC.md 에 정의된 X/Thread 스타일 bubble UI (grouping · date separator · 2종 toast · reduced-motion · a11y) 를 MemoSection 내부 inline 스타일로 구현한다.

Purpose:
- 2026-04-22 프로덕션에서 발생한 "ghost cursor" 증상의 근본 제거 (수신측 textarea 잔상). 근본 원인은 debug 세션 kkuk-memo-send-noop Evidence E-I 에 기록됨.
- 사용자 요구 UX (X/Thread 수준의 깔끔한 양방향 채팅) 반영.
- MEMO-CLEANUP-01 의 UI-layer 부분을 v1.1 원래 계획보다 조기 완료 (write-path 제거 · 테이블 DROP 은 v1.1 에 여전히 남김).

Output: src/App.jsx 단일 파일 수정 (decomposition 금지 유지). 단일 atomic commit. codex review PASS 필수 (MEMO-FIX-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.5-memo-ux-cleanup/05.5-CONTEXT.md
@.planning/phases/05.5-memo-ux-cleanup/05.5-UI-SPEC.md
@.planning/debug/kkuk-memo-send-noop.md
@CLAUDE.md

<!-- App.jsx 내 작업 대상 라인 범위. 이 범위 외 영역은 절대 수정 금지 (CLAUDE.md monolith 정책). -->
<line_ranges>
- src/App.jsx:2059-2198  — MemoSection 컴포넌트 본체 (textarea 제거 + bubble UI 재구성 + composer 스타일 업데이트 + empty state + onboarding toast + send-failure toast 모두 이 범위 안에서)
- src/App.jsx:4664-4678  — onMemosChange Realtime 핸들러 (skip 로직 narrow or 제거)
- src/App.jsx:6740-6787  — MemoSection 호출부 props (onMemoChange/onMemoBlur/onMemoSend + upsertMemo 호출 제거). memoValue/onMemoChange prop 은 signature 에서 완전 삭제 또는 no-op 로 축소
- src/App.jsx:6789-6811  — onReplySubmit (send-failure catch 분기 추가만, 기존 경로 불변)
</line_ranges>

<!-- 건드리지 말 것. -->
<forbidden_ranges>
- src/App.jsx:5519-5535  — analyzeMemoSentiment (ai-child-monitor 401 은 별도 세션; 본 phase scope 외)
- src/lib/sync.js  — subscribeFamily 구조 불변 (memos 채널 자체는 남김: fetchMemos polling 이 archive 용으로 계속 필요)
- 그 외 App.jsx 전체 (KKUK, SOS, RL, subscription, events, academies 경로 일체)
</forbidden_ranges>

<!-- 현재 MemoSection signature + 호출 사이트 요약 (executor 가 재탐색할 필요 없도록). -->
<interfaces>
현재 MemoSection 시그니처 (App.jsx:2059):
```jsx
function MemoSection({ memoValue, onMemoChange, onMemoBlur, onMemoSend, replies, onReplySubmit, readBy, myUserId, isParentMode, onReplyRef })
```

props 의미:
- memoValue:  legacy memos[dateKey] 문자열 — 이 phase 에서 **textarea 완전 제거 후 미사용**. signature 에서 drop.
- onMemoChange/onMemoBlur/onMemoSend: upsertMemo 호출 wrappers — **signature drop**.
- replies:  memoReplies 배열. shape = { id, user_id, user_role, content, created_at, origin?, read_by? }. **유지**.
- onReplySubmit(text): 현재 단일 인자. **에러 콜백 확장**: `onReplySubmit(text, { onError })` 또는 executor 판단으로 Promise 를 return 하도록 호출부(App.jsx:6789) 변경. 선택 자유, UI-SPEC 제약 없음.
- readBy, myUserId, isParentMode, onReplyRef: **유지**.

수신 핸들러 (onMemoRepliesChange, App.jsx:4740-4747): 자기 echo 는 이미 skip 중 (`newRow.user_id === authUser?.id` return). 이 경로는 정상이므로 건들 필요 없음.

sendMemo 호출부 (App.jsx:6801):
```js
sendMemo(familyId, dateKey, content, authUser.id, myRole, origin)
  .then(() => fetchMemoReplies(familyId, dateKey).then(setMemoReplies))
  .catch(err => console.error("[reply]", err));
```
→ 현재 catch 가 console 로만 조용히 삼킴. 이 phase 에서 **catch → send-failure toast state 활성화** 로 연결.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Legacy textarea + upsertMemo 쓰기 경로 완전 제거</name>
  <files>src/App.jsx</files>
  <behavior>
    - MemoSection 함수 signature 에서 memoValue, onMemoChange, onMemoBlur, onMemoSend 4개 prop 삭제 (per MEMO-FIX-01).
    - App.jsx:2083-2144 "Daily memo body" 전체 `<div>` 블록(textarea + 역할 라벨 + helper text + "메모 보내기" 버튼) 삭제.
    - App.jsx:6740-6787 호출부에서 memoValue/onMemoChange/onMemoBlur/onMemoSend prop 4개 완전 제거. 관련 closure 내부의 `upsertMemo(familyId, dateKey, ...)` 호출 2곳(App.jsx:6749, 6758, 6774) 전부 삭제.
    - 연계된 ref (memoDirty, memoLastValue, memoSaveTimer) 중 더 이상 참조되지 않는 것은 선언부까지 제거 (dead-code cleanup). sendInstantPush new_memo 알림 코드도 onMemoBlur/onMemoSend 내부에 있었으므로 함께 삭제 (reply 경로의 sendInstantPush 는 App.jsx:6804 에 별도로 존재하므로 알림 기능 자체는 유지됨 — per MEMO-FIX-01 scope 는 legacy write path).
    - onMemosChange handler (App.jsx:4664-4678) 의 `if (newRow.date_key === dateKeyRef.current) return;` guard (line 4671) 을 (a) 완전 제거 **또는** (b) `if (newRow.user_id === authUser?.id) return;` (self-echo 만 skip) 로 narrow. UI-SPEC "Backward-Compatibility Note" §recommended sequencing 3번에 따라 (b) 를 권장하지만 textarea 가 이미 제거됐으므로 (a) 도 안전. executor 선택. 선택 근거를 commit message 에 기재.
    - `memos` state object 자체와 `fetchMemos` polling 은 **유지** (UI-SPEC §Backward-Compatibility 명시: archive/date navigation 용으로 계속 필요; 완전 제거는 v1.1 MEMO-CLEANUP-01 의 테이블 DROP 시점으로 이연).
    - 검증 grep: `grep -rn "upsertMemo(" src/App.jsx` 결과 0 라인. (sync.js 의 export 는 남김 — v1.1 에서 rm)
    - 검증 grep: `grep -n "memoValue\|onMemoChange\|onMemoBlur\|onMemoSend" src/App.jsx` 결과 0 라인.
    - **CLAUDE.md monolith 정책 준수**: src/App.jsx 외 파일 생성/분해 금지. src/App.jsx 내 편집은 위 <line_ranges> 범위 안에서만.
    - 이 단계 완료 시점: UI 는 "Daily memo body 가 사라지고 기존 bubble 리스트 + composer 만 남은 상태". 다음 Task 2 에서 bubble UX 고도화.
    - 아직 commit 하지 말 것 (Task 3 에서 단일 atomic commit).
  </behavior>
  <action>
    1. src/App.jsx:2059 MemoSection signature 변경 → `function MemoSection({ replies, onReplySubmit, readBy, myUserId, isParentMode, onReplyRef })` (4개 prop drop).
    2. src/App.jsx:2062 `const memoText = memoValue || "";` 라인 + 2073 `const hasMemo = ...` 라인 삭제.
    3. src/App.jsx:2083-2144 "Daily memo body" 전체 `<div style={{ padding: "14px 16px", background: "#FFF7FB"...}}>` 블록 완전 삭제.
    4. src/App.jsx:6740-6787 호출부에서 memoValue / onMemoChange / onMemoBlur / onMemoSend 4 props 전체 제거 (props 로 넘기던 내부 closure 및 그 안의 upsertMemo 호출 전부 포함).
    5. 더 이상 참조되지 않는 ref 선언부 (memoDirty, memoLastValue, memoSaveTimer) 찾아 제거. (grep 으로 검색 후 remaining reference 0 확인)
    6. src/App.jsx:4671 `if (newRow.date_key === dateKeyRef.current) return;` 삭제 **또는** `if (newRow.user_id === authUser?.id) return;` 로 교체 (per MEMO-FIX-02). 선택한 쪽을 commit message 에 한 줄 justification 포함.
    7. src/App.jsx 에 대해 `npm run build` 실행 — 에러 없어야 함. (textarea 제거 후 unused imports 나 unused helpers 가 남았다면 제거.)
    8. `grep -rn "upsertMemo(" src/App.jsx` 실행 → 0 라인 확인. (정의 자체는 src/lib/sync.js 에 남음 — v1.1 제거 예정이므로 여기서 건들지 않음.)
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -Ei "error|failed" | grep -v "0 errors"; echo "---"; grep -rn "upsertMemo(" src/App.jsx; echo "---"; grep -n "memoValue\|onMemoChange\|onMemoBlur\|onMemoSend" src/App.jsx</automated>
  </verify>
  <done>
    - `npm run build` 가 에러 없이 통과.
    - `grep -rn "upsertMemo(" src/App.jsx` 가 0 라인.
    - `grep -n "memoValue\|onMemoChange\|onMemoBlur\|onMemoSend" src/App.jsx` 가 0 라인.
    - dev server (`npm run dev`) 에서 MemoSection 에 textarea 가 보이지 않고 기존 bubble + composer 만 보인다 (수동 spot check — UI-SPEC 적용은 Task 2 에서).
    - 아직 commit 안 됨.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: X/Thread 스타일 bubble UI · grouping · date separator · empty state · 2 toasts · a11y · reduced-motion 구현 (UI-SPEC 준수)</name>
  <files>src/App.jsx</files>
  <behavior>
    이 task 는 05.5-UI-SPEC.md §4 (Chat Bubble Area) · §5 (Composer Bar) · §6 (Onboarding Toast) · §7 (Send-Failure Toast) · §Interaction Contracts · §Accessibility Contract · §Copywriting Contract 를 **모두** MemoSection 내부 inline 스타일로 구현한다. 아래 체크리스트는 UI-SPEC 과 1:1 매핑되며 executor 는 각 항목 옆에 참조 SPEC 섹션 주석을 남겨 reviewer 가 대조 가능해야 한다 (예: `/* UI-SPEC §4a Date Separator Pill */`).

    **SPEC 체크리스트 (모두 구현 필수):**

    [§3 Design Decisions]
    - [ ] 모든 inline style 이 UI-SPEC Color table 에 등재된 값만 사용 (예: 내 bubble #E879A0, 상대 #F3F4F6/#374151, legacy #FFF7ED/#92400E/#FBBF24 dashed, 날짜 텍스트 #9CA3AF, 라인 #E5E7EB, 실패 토스트 #FEE2E2/#991B1B)
    - [ ] 현재 App.jsx:2079 섹션 헤더 `fontWeight: 800` 을 UI-SPEC Typography 표에 맞춰 **700** 으로 교정
    - [ ] 현재 App.jsx:2193 send button width/height 40→**44**px (UI-SPEC §5 touch target 44x44)

    [§4a Date Separator Pill] — replies 배열을 순회하며 직전 message 와 `r.created_at` 의 YYYY-MM-DD 가 다르면 separator 삽입
    - [ ] date 계산: `const dk = r.created_at.slice(0,10); const prevDk = prev?.created_at?.slice(0,10)` (or toLocaleDateString KO)
    - [ ] label 생성: 오늘 → "오늘", 어제 → "어제", 그 외 → `new Date(r.created_at).toLocaleDateString('ko-KR', { month:'long', day:'numeric', weekday:'long' })`
    - [ ] markup: `<div style={{display:'flex', alignItems:'center', margin:'8px 0'}}><hr .../><span style={{padding:'3px 12px',borderRadius:99,background:'#F3F4F6',fontSize:10,color:'#9CA3AF',fontWeight:700,margin:'0 8px',whiteSpace:'nowrap'}}>{label}</span><hr .../></div>`
    - [ ] `role="separator" aria-label={label}`

    [§4b Message Group Logic] — 같은 sender 이고 연속 message 간 `created_at` 간격 ≤ 180000ms 이며 date 가 안 바뀌면 같은 group
    - [ ] group 의 첫 bubble 에서만 avatar 렌더
    - [ ] group 의 중간 bubble 은 `<div style={{width:28,flexShrink:0}} aria-hidden="true" />` spacer
    - [ ] group 의 마지막 bubble 에서만 timestamp 렌더
    - [ ] 마지막 bubble marginBottom: 12px, 중간: 4px

    [§4c Avatar] — 기존 App.jsx:2154-2155 색상·글립 유지 (#DBEAFE👩 / #FCE7F3🐰 / #FEF3C7👶). 크기 28x28, borderRadius 14.

    [§4d Bubble] — 기존 App.jsx:2158-2170 스타일 유지하되 padding `10px 14px`, fontSize 14, lineHeight **1.45** (현재 1.5 → 1.45 로 교정), fontFamily FF, wordBreak/overflowWrap 추가. borderRadius rule 유지 (내 "16px 4px 16px 16px" · 상대 "4px 16px 16px 16px" · legacy 12px).

    [§4e Timestamp] — 마지막 bubble 에만 렌더.
    - [ ] relative time: <60s → "방금", 60s–59min → "N분 전", 1h–23h59 → "N시간 전", ≥24h 같은 해 → "어제 오후 3:12" 또는 "4월 15일 오후 3:12", 다른 해 → "2025. 4. 15. 오후 3:12"
    - [ ] fontSize 10, color #9CA3AF, marginTop 4, textAlign isMe ? 'right' : 'left'
    - [ ] 실시간 tick 불필요 (render time 계산 1회)

    [§4f Entry Animation]
    - [ ] `useRef(new Set())` 로 "이미 본 id" 집합 유지. Set 에 없는 id 는 새 message → `animation: 'bubbleIn 150ms ease-out forwards'` 추가. 렌더 후 집합에 추가.
    - [ ] `<style id="memo-bubble-anim">@keyframes bubbleIn { from {opacity:0; transform:translateY(4px);} to {opacity:1; transform:translateY(0);} } @keyframes toastIn { from {opacity:0; transform:translateX(-50%) translateY(8px);} to {opacity:1; transform:translateX(-50%) translateY(0);} }</style>` 를 MemoSection 최상위 `<div>` 바로 안쪽(role="log" 위)에 렌더 (idempotent: `document.getElementById('memo-bubble-anim')` 체크 후 삽입 or React 상 `<style>` 태그로 곧바로 마크업에 포함)
    - [ ] `const prefersReducedMotion = useMemo(() => typeof window !== 'undefined' && window.matchMedia('(prefers-reduced-motion: reduce)').matches, [])` → true 면 animation 속성 자체를 부여하지 않음

    [§4g Empty State] `replies.length === 0` 일 때
    - [ ] `<div style={{textAlign:'center', padding:'24px 16px', color:'#D1D5DB'}}>`
    - [ ] `💗` 32px + marginBottom 8
    - [ ] "아직 주고받은 메시지가 없어요" fontSize 14 fontWeight 700 color #9CA3AF
    - [ ] role 별 prompt: parent → "아이에게 첫 메시지를 남겨보세요 💗" / child → "부모님께 오늘 하루를 전해봐~ 🐰" (fontSize 13, color 상속)

    [§4 Bubble Container a11y]
    - [ ] `<div role="log" aria-live="polite" aria-label="메모 대화">` 로 bubble 목록을 감쌈
    - [ ] 각 bubble wrapper `<div>` 에 `role="article"` + `aria-label="{sender_label} {relative_time}에 보낸 메시지: {content}"` (sender_label: parent→"부모님", child→"아이", legacy→"예전 메모", isMe→"나")

    [§5 Composer Bar]
    - [ ] 기존 App.jsx:2183-2196 스타일을 UI-SPEC §5 값으로 swap
    - [ ] `paddingBottom: max(10px, env(safe-area-inset-bottom))` 지원 (CSS inline 작성법: `paddingBottom: 'max(10px, env(safe-area-inset-bottom))'`)
    - [ ] input: border 1.5px #E5E7EB → focus 시 1.5px #E879A0 (useState isFocused + onFocus/onBlur handler). borderRadius 22, padding `11px 16px`, fontSize **16** (iOS 자동 줌 방지).
    - [ ] send button: 44x44px, borderRadius 22, background inputText.trim() ? gradient : #E5E7EB, fontSize 18, cursor input.trim() ? pointer : default
    - [ ] mobile 감지 inline: `const isMobile = /Android|iPhone|iPad/i.test(navigator.userAgent) || (typeof window !== 'undefined' && window.Capacitor !== undefined);` → Enter 처리 분기 (desktop: Enter=send, mobile: Enter=newline)
    - [ ] input aria-label: parent mode 는 "메모 입력", child mode 는 "답글 입력"
    - [ ] send button aria-label "메시지 보내기" type="button"
    - [ ] autoComplete="off" autoCorrect="on" spellCheck="true"

    [§6 Onboarding Toast]
    - [ ] `useState(false)` for showOnboardingToast. mount 시 `useEffect(() => { if (!localStorage.getItem('memoOnboardingV2Seen')) { setShowOnboardingToast(true); localStorage.setItem('memoOnboardingV2Seen','1'); const t = setTimeout(() => setShowOnboardingToast(false), 4000); return () => clearTimeout(t); } }, [])`
    - [ ] 렌더 위치: MemoSection 카드 바깥 `<></>` fragment 에 나란히 (또는 `createPortal` 은 monolith 정책상 피하고 카드 내부 `position: fixed` 로 처리). position fixed, bottom max(80px, 80px + env(safe-area-inset-bottom)), left 50%, transform translateX(-50%), background #E879A0, color white, borderRadius 24, padding 12 20, fontSize 14, fontWeight 700, zIndex 9999
    - [ ] animation: `prefersReducedMotion` 이 false 일 때만 `toastIn 200ms ease-out forwards`
    - [ ] 문구: "메모 화면이 새로워졌어요 ✨"
    - [ ] dismiss 버튼 "×" (aria-label "닫기", 44x44 terminatable — minWidth/minHeight 24)
    - [ ] `role="status" aria-live="polite" aria-label="메모 화면이 새로워졌어요"`

    [§7 Send-Failure Toast]
    - [ ] `useState(false)` for showSendFailureToast + `useState(null)` for lastFailedText. onReplySubmit 이 reject 되면 setShowSendFailureToast(true) + setLastFailedText(text). 5초 auto-dismiss.
    - [ ] position 동일 레이아웃, background #FEE2E2, color #991B1B
    - [ ] `role="alert" aria-live="assertive" aria-label="메시지 전송 실패"`
    - [ ] 본문: "메시지 전송에 실패했어요. 다시 시도해 주세요."
    - [ ] "다시 시도" 버튼 (inline, border 1px #991B1B, borderRadius 12, padding 3 10, fontWeight 700, aria-label "메시지 전송 다시 시도") → onClick: lastFailedText 를 다시 handleSend/onReplySubmit 으로 전달
    - [ ] 닫기 "×" 버튼 aria-label "닫기"
    - [ ] 호출부(App.jsx:6789-6811 onReplySubmit)에서 sendMemo().catch 를 기존 `console.error("[reply]", err)` **유지 + 추가로** props 로 전달된 에러 콜백(or 리턴된 Promise)을 통해 toast 발동. executor 는 두 가지 방법 중 하나를 선택:
      - Option A: `onReplySubmit` signature 를 `(text) => Promise` 로 변경하고 MemoSection 의 handleSend 가 await 후 .catch 로 toast 발동
      - Option B: props 에 `onReplyError: (text, err) => void` 추가하여 onReplySubmit 의 .catch 에서 호출
      선택 근거 한 줄 commit message 에 남김

    [§Interaction §Scroll-to-Bottom]
    - [ ] `useRef` for MemoSection outer div → `containerRef`. 새 message 추가 감지 시 `containerRef.current?.scrollIntoView({ behavior: prefersReducedMotion ? 'instant' : 'smooth', block: 'end' })`. 감지 mechanism: replies.length 의 이전 값을 `useRef` 로 비교.

    [§Copywriting Contract] — 위 체크박스에 분산됨. 상단 섹션 헤더는 "💬 오늘의 메모" 유지 (내용 변경 없음, fontWeight 만 700 으로).

    **SPEC 주석 rule**: 위 체크리스트 각 항목 옆에 `/* UI-SPEC §X.X */` 주석을 JSX 에 남겨 codex review 시 1:1 대조가 가능하게 한다.

    **dark mode 금지**: UI-SPEC §Design Decisions 행 "Dark mode: skip" — `@media (prefers-color-scheme: dark)` 관련 스타일 일체 추가 금지.

    **새 npm dep 금지**: CLAUDE.md + UI-SPEC §Registry Safety — framer-motion, emotion, styled-components, react-spring, date-fns 등 모두 금지. animation 은 @keyframes, date format 은 `toLocaleDateString/toLocaleTimeString('ko-KR', ...)`.

    **monolith rule**: 추가 코드는 모두 MemoSection 함수 내부 또는 그 직계 helper (MemoSection 파일 내 같은 scope) 로 한정. 신규 파일 생성 금지. 외부 export 금지.

    **line budget**: MemoSection 블록 (App.jsx:2059-2198) 이 약 140 라인 → 최종 200~300 라인 사이가 정상. 400 라인 넘으면 action 이 과했는지 재점검. 단 필요한 SPEC 체크리스트 항목은 절대 축소하지 말 것 (scope reduction prohibition 적용 — 어려워 보인다고 생략 금지).

    아직 commit 하지 말 것 (Task 3 에서 단일 atomic commit).
  </behavior>
  <action>
    1. App.jsx:2059-2198 MemoSection 전체를 위 체크리스트에 맞게 재작성. 각 SPEC 항목마다 `/* UI-SPEC §X.X */` 인라인 JSX 주석.
    2. helper 함수 (MemoSection 상단에 inline) 정의:
       - `getRelativeTime(createdAt)` — "방금/N분 전/N시간 전/어제 오후.../4월 15일 오후.../2025. 4. 15. 오후..." 분기 반환
       - `getDateSeparatorLabel(createdAt)` — "오늘/어제/4월 20일 월요일" 분기 반환
       - `groupMessages(replies)` — replies 를 date + group 으로 flatten → `[{type:'separator', label}, {type:'bubble', r, isFirstInGroup, isLastInGroup, isMe, animateIn}]` 배열. 같은 sender + 180000ms 이내 + 같은 date 면 동일 group.
    3. bubble 렌더 루프를 groupMessages 결과 기반으로 재작성. `role="log"` wrapper + `role="article"` 각 bubble + `aria-label` 정확 포맷.
    4. `<style>` 블록 `@keyframes bubbleIn` + `@keyframes toastIn` 삽입 (MemoSection JSX 최상단에 `<style id="memo-bubble-anim">...</style>`; React 가 중복 렌더해도 동일 id 이므로 브라우저가 병합 — idempotent).
    5. composer (App.jsx:2183-2196) 를 UI-SPEC §5 에 맞게 스타일 교체. input focus state + mobile 감지 + aria-label 추가. Enter 처리 분기.
    6. 2 toast 렌더 추가 (onboarding + send-failure). 렌더 위치는 MemoSection return 의 outermost fragment 내에 조건부 렌더 (`{showOnboardingToast && <div .../>} {showSendFailureToast && <div .../>}`).
    7. App.jsx:6789-6811 onReplySubmit 을 Promise return 스타일로 변경 (**또는** `onReplyError` prop 추가) 해서 MemoSection 이 reject 를 캐치할 수 있게 wiring. sendMemo().catch 내부의 console.error 는 유지하되 그 아래에서 caller 에게 에러를 다시 throw (Option A) 또는 onReplyError 호출 (Option B).
    8. 사용하지 않게 된 `hasMemo` / `memoSent` local state (Task 1 에서 아직 남아있다면) 제거.
    9. `npm run build` 통과 확인. TypeScript 없음 (JSX), ESLint warning 만 체크.
    10. `grep -rn "upsertMemo" src/App.jsx` = 0 재확인.
    11. 로컬 `npm run dev` 에서 시각 검증:
        - 1 tab: parent 세션 → 메시지 3개 연속 송신 → 3개가 하나의 group 으로 avatar 1회 + 마지막 timestamp 1회
        - 2 tab: child 세션 (다른 계정) → parent bubble 이 좌측에 Realtime 도착 → onboarding toast 가 최초 방문 시 1회
        - 2번째 방문에선 onboarding toast 미표시 (localStorage 확인)
        - 날짜 변경 재현을 위해 `r.created_at` 을 임시로 과거 날짜로 조작해봐도 date separator 가 뜨는지 (개발자 도구 콘솔에서 memoReplies state 조작) — optional
        - 네트워크 끊은 뒤 전송 → send-failure toast + retry button. retry 누르면 재전송 시도.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20; echo "---grep upsertMemo---"; grep -rn "upsertMemo(" src/App.jsx; echo "---grep SPEC anchors---"; grep -c "UI-SPEC" src/App.jsx; echo "---grep keyframes---"; grep -n "bubbleIn\|toastIn\|memo-bubble-anim" src/App.jsx; echo "---grep a11y---"; grep -n "role=\"log\"\|role=\"article\"\|role=\"alert\"\|role=\"status\"\|aria-live" src/App.jsx; echo "---grep colors---"; grep -n "#FEE2E2\|#991B1B\|#E879A0\|memoOnboardingV2Seen" src/App.jsx</automated>
  </verify>
  <done>
    - `npm run build` 통과.
    - `grep -rn "upsertMemo(" src/App.jsx` → 0.
    - `grep -c "UI-SPEC" src/App.jsx` ≥ 15 (SPEC 섹션별 주석이 최소 15개 이상 남아있음).
    - `grep -n "bubbleIn" src/App.jsx` ≥ 1, `grep -n "toastIn" src/App.jsx` ≥ 1, `grep -n "memo-bubble-anim" src/App.jsx` ≥ 1.
    - `grep -n "role=\"log\"\|role=\"article\"\|role=\"alert\"\|role=\"status\"" src/App.jsx` 각각 ≥ 1.
    - `grep -n "#FEE2E2" src/App.jsx` ≥ 1 (send-failure toast 색상), `grep -n "memoOnboardingV2Seen" src/App.jsx` ≥ 1.
    - 로컬 dev 실시간 smoke: 2 tabs (다른 계정) 에서 메모 송수신 시 양측 화면에 bubble 이 3초 이내 나타남, textarea 잔상 없음, grouping/timestamp/date separator 모두 SPEC 대로, onboarding toast 1회만.
    - 아직 commit 안 됨 (Task 3 에서 atomic commit).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Unit test verification + codex review gate + atomic commit</name>
  <files>src/App.jsx</files>
  <behavior>
    모든 기존 unit test 가 그대로 통과하는지 확인 (회귀 없음 보장). 그 후 commit 을 만들기 전에 **반드시 `/codex review` 를 실행** 하여 diff 를 통과시킨다 (per MEMO-FIX-05 + 2026-04-22 user feedback memory "codex review gate").

    codex review 가 FAIL 하면 지적 사항 수정 → 다시 review → PASS 될 때까지 반복. PASS 후 단일 atomic commit.

    E2E 테스트 작성은 본 plan 의 scope 아님 (05.5-02-PLAN.md 에서 수행). 본 task 는 unit 만.
  </behavior>
  <action>
    1. `npm run test` 실행 — 전체 Vitest suite PASS 확인. FAIL 이 있으면 회귀이므로 수정. (본 phase 가 touch 한 코드 경로 외 test 가 FAIL 하면 unrelated pre-existing issue 로 commit message 에 기록.)
    2. 작업 트리 diff 확보: `git diff src/App.jsx`
    3. `/codex review` 호출 (또는 해당 workflow 의 equivalent). review 피드백 수용 → 수정 → 재검. PASS 될 때까지 반복. 각 round 의 codex 요약을 commit message 말미에 한 줄 기록.
    4. PASS 확인 후 atomic commit:

       ```
       feat(5.5-01): remove legacy memo textarea and ship X/Thread bubble UI

       - Drop MemoSection legacy `<textarea>` + upsertMemo write-path (MEMO-FIX-01)
         · src/App.jsx:2083-2144 "Daily memo body" block removed
         · src/App.jsx:6740-6787 memoValue/onMemoChange/onMemoBlur/onMemoSend props removed
         · upsertMemo() call sites: 0 remaining in src/App.jsx (grep verified)
       - Narrow onMemosChange Realtime skip guard (MEMO-FIX-02)
         · src/App.jsx:4671 skip now <fully removed | own-user only — justify which and why>
       - Implement X/Thread-grade chat bubble UX per 05.5-UI-SPEC.md (MEMO-FIX-03)
         · group consecutive same-sender messages within 180s window (§4b)
         · date separator pill with 오늘/어제/Korean-long format (§4a)
         · relative timestamps 방금/N분 전/N시간 전/absolute (§4e)
         · bubbleIn 150ms entry animation + prefers-reduced-motion guard (§4f)
         · empty state with 💗 + role-specific prompts (§4g)
         · composer: 44px touch target, 16px input (iOS auto-zoom prevention), #E879A0 focus border (§5)
         · role="log" aria-live="polite" bubble container + role="article" per bubble (§Accessibility)
       - Add send-failure toast: role=alert aria-live=assertive, #FEE2E2/#991B1B, retry button (§7)
       - Add one-time onboarding toast: #E879A0 brand pink, localStorage.memoOnboardingV2Seen, 4s auto-dismiss (§6)

       Root cause reference: .planning/debug/kkuk-memo-send-noop.md Evidence E-I
       Monolith rule: src/App.jsx edits confined to line ranges 2059-2198, 4664-4678, 6740-6811.
       No new npm deps. No dark-mode styles. KKUK/SOS/RL paths untouched. No schema changes.

       codex review: PASS (round N)
       Refs: MEMO-FIX-01, MEMO-FIX-02, MEMO-FIX-03, MEMO-FIX-05
       ```

    5. commit 후 `git show --stat HEAD` 로 단일 파일(src/App.jsx) 만 변경되었는지 확인.
  </action>
  <verify>
    <automated>npm run test 2>&1 | tail -20; echo "---"; git log -1 --stat; echo "---"; git show HEAD --stat | grep -c "src/App.jsx"</automated>
  </verify>
  <done>
    - `npm run test` 가 전부 PASS.
    - `/codex review` 가 PASS 로 통과 (round 횟수 관계 없음).
    - 단일 atomic commit 이 존재하며 `git show HEAD --stat` 이 `src/App.jsx` 1 파일만 포함.
    - commit message 에 MEMO-FIX-01/02/03/05 4개 requirement ID 가 명시됨.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM → React state | 사용자가 composer input 에 타이핑한 텍스트가 setState 로 흘러감. 현재 기존 경로 (onReplySubmit → sendMemo) 재사용이므로 새 경계 아님. |
| localStorage → React | memoOnboardingV2Seen 플래그 읽기/쓰기. 사용자 자기 브라우저만 접근; 변조되어도 "토스트 1회 더 뜸/안 뜸" 수준. |
| Supabase Realtime → onMemosChange | 외부 INSERT event payload 를 받아 `setMemos` 업데이트. 이미 sync.js 구독 contract 로 검증된 경로. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5.5.01-01 | Tampering (T) | `localStorage.memoOnboardingV2Seen` | accept | 값이 위조되어도 "온보딩 토스트가 한 번 더 뜨거나 안 뜸" 수준의 UX cosmetic 영향. 보안 자산 아님. |
| T-5.5.01-02 | Information Disclosure (I) | Bubble `aria-label` 이 message content 를 그대로 포함 | accept | 해당 메시지는 동일 DOM 트리의 visible text 이기도 함. 스크린 리더 사용자에게 숨길 이유 없음. family 외부 노출 경로 없음. |
| T-5.5.01-03 | Denial of Service (D) | `bubbleIn` animation + 대량 replies 렌더 | mitigate | animation 은 150ms one-shot, ref set 으로 중복 방지. replies 수가 많을 경우 grouping 이 DOM 노드를 오히려 줄인다 (avatar/timestamp 생략). `prefers-reduced-motion` 존중. |
| T-5.5.01-04 | Tampering (T) | send-failure retry button 이 lastFailedText 를 다시 송신 | accept | 서버측 중복 방지는 이미 memo_replies insert 의 사용자-의도 특성상 수용 가능 (사용자가 한 번 더 보내기를 선택). idempotency key 불필요 — UX 상 retry 는 명시적 사용자 행동. |
| T-5.5.01-05 | Information Disclosure (I) | onMemosChange guard narrow 시 Realtime payload 가 memos state 갱신 | accept | `memos` 테이블은 같은 family_id 의 RLS 정책으로 이미 보호됨 (Phase 4 SEC). 가드 narrow 는 단지 "수신자 자기 화면을 갱신할지" 결정하는 UI-layer 문제. 외부 유출 아님. |
| T-5.5.01-06 | Repudiation (R) | textarea 제거 후 `public.memos` row 변경 이력 유지 여부 | accept | v1.1 MEMO-CLEANUP-01 까지 `public.memos` 테이블 자체는 유지되며 read-mostly 로 전환됨. 본 phase 는 write-path 제거만 수행하므로 감사 로그 완전성 영향 없음. |

모든 threat 가 low risk & 기존 경로 재사용 — 신규 mitigation 코드 추가는 불필요. pure UI-layer refactor 의 성격.
</threat_model>

<verification>
1. `npm run build` 에러 없이 통과
2. `npm run test` (Vitest) 전체 PASS
3. `grep -rn "upsertMemo(" src/App.jsx` 결과 0 라인
4. `grep -n "memoValue\|onMemoChange\|onMemoBlur\|onMemoSend" src/App.jsx` 결과 0 라인
5. `grep -c "UI-SPEC" src/App.jsx` ≥ 15 (SPEC 섹션 주석이 diff 전반에 남아있음)
6. `grep -n "role=\"log\"\|role=\"article\"\|role=\"alert\"\|role=\"status\"" src/App.jsx` 전부 ≥ 1
7. `grep -n "bubbleIn\|toastIn\|memo-bubble-anim\|memoOnboardingV2Seen\|#FEE2E2\|#991B1B" src/App.jsx` 6 종 전부 ≥ 1
8. 2-tab 로컬 smoke (parent + child 별개 계정): 양측에서 메시지 3초 이내 반영, textarea 없음, grouping·timestamp·date separator·empty state·2 toast 모두 육안 확인
9. `/codex review` PASS
10. `git show HEAD --stat` 이 src/App.jsx 1개 파일만 변경
</verification>

<success_criteria>
- 단일 atomic commit 이 merge 대기 상태
- MEMO-FIX-01 · MEMO-FIX-02 · MEMO-FIX-03 · MEMO-FIX-05 4개 requirement 가 구현됨 (MEMO-FIX-04 는 05.5-02-PLAN.md 에서 E2E 로 검증)
- CLAUDE.md monolith 정책 준수: src/App.jsx 외 파일 변경 없음, App.jsx 내 편집은 선언된 line range 외부로 새지 않음
- 05.5-UI-SPEC.md 모든 SPEC 섹션 (Spacing·Typography·Color·Component Inventory·Interaction·Accessibility·Copywriting) 이 구현에 1:1 반영됨 (주석으로 역추적 가능)
- 신규 npm dep 0
- Supabase schema 변경 0
- codex review PASS
</success_criteria>

<output>
After completion, create `.planning/phases/05.5-memo-ux-cleanup/05.5-01-SUMMARY.md` with:
- Commit SHA 및 line delta (+X/-Y)
- SPEC 체크리스트 완수 항목 리스트
- onMemosChange 가드를 "완전 제거" / "own-user only" 중 어느 쪽으로 narrow 했는지 + 근거
- onReplySubmit error 전달 방식을 Option A (Promise) / Option B (onReplyError prop) 중 어느 쪽으로 택했는지 + 근거
- codex review round 수 및 주요 피드백 요약
- 로컬 2-tab smoke 결과 (간단 캡처 or 서술)
</output>
