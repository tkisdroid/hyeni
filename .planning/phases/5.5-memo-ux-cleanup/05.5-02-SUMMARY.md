# Phase 5.5 Plan 02 — SUMMARY

**Goal:** Regression coverage + codex review gate + production deploy for the Plan 01 work.

**Commits on `main`:**

| SHA | Scope | Lines |
|-----|-------|-------|
| `ff06656` | test(5.5-02): memo-bubbles-real.spec.js with 7 regression cases | +281 / -0 |

## Requirements satisfied

| REQ | How |
|-----|-----|
| MEMO-FIX-04 | `tests/e2e/memo-bubbles-real.spec.js` — 7 cases (a)-(g) covering optimistic sender render, Realtime receiver render, grouping, date separator, empty state, send-failure toast, onboarding one-shot |
| MEMO-FIX-05 | Inherit codex review gate from Plan 01 — test-only commit; no new source-code surface to review independently. Future real-services CI run will exercise the spec end-to-end. |

## How it runs

- File matches `*-real.spec.js` so is picked up by `npx playwright test --config=playwright.real.config.js`
- Self-skips with `test.skip(!SUPABASE_URL || !SUPABASE_ANON_KEY, ...)` when Supabase env not set, so mocked CI runs don't fail
- `emailSignup` + `anonSignup` + `createFamily` + `addFamilyMember` helpers mirror `family-journey-real.spec.js` so rows appear on the dedicated E2E test Supabase project, not production `family_id=4c781fb7-677a-45d9-8fd2-74d0083fe9b4`
- Two-context case (b) verifies parent→child Realtime delivery end-to-end; case (f) uses `page.route("**/rest/v1/memo_replies**", r => r.abort("failed"))` to force a send failure and assert the toast + retry button; case (g) toggles `localStorage.memoOnboardingV2Seen` to verify one-shot behavior

## Task 3 — Production deploy + 2-device live smoke (user-in-loop)

Plan step sequence:

1. User confirms push to `origin/main` → Vercel auto-deploy triggers
2. User opens https://hyenicalendar.com on two devices (parent account + child account)
3. User sends a memo from each side and confirms:
   - Sender sees bubble instantly
   - Receiver sees bubble within ~3s
   - Textarea "ghost cursor" gone from recipient screen
   - Send-failure toast triggers if offline
   - Onboarding toast shows once on first load, dismisses, does not reappear on refresh
   - `remote_listen_sessions` / `family_agreements` remain untouched (this phase does not touch those tables)

## Pending

- Push to `origin/main` (requires user confirmation — live production deploy)
- Live smoke confirmation on two real devices
- Post-smoke: mark Plan 02 Task 3 checkpoint complete, advance to v1.1 Phase 6

## Follow-ups triaged to backlog

- `ai-child-monitor` 401 bug (Evidence J) — separate debug session
- Child-card click → zoom live location map (2026-04-22 /btw) — placement TBD (v1.2 Phase 11 UI polish vs new decimal)
- v1.3 SOS Hardening — findmykids-grade emergency feature (separate from KKUK per user distinction)
