<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- Phase 5 D-B06 (RL-03): required for AmbientListenService on API 34+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- QR pairing scanner: WebView getUserMedia({video}) needs CAMERA.
         Bundled into the first-launch permission prompt so child onboarding
         is uninterrupted when the QR scanner button is tapped. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Phase 5.x (force_ring): emergency parental alert FGS + alarm volume control -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="isMonitoringTool"
            android:value="child_monitoring" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="hyeni_schedule_v4" />

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep link: OAuth callback from system browser -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="hyenicalendar" android:host="auth-callback" />
            </intent-filter>

        </activity>

        <activity
            android:name=".PushAlertActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTop"
            android:showWhenLocked="true"
            android:theme="@style/AppTheme.Alert"
            android:turnScreenOn="true" />

        <activity
            android:name=".RemoteListenActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTop"
            android:showWhenLocked="true"
            android:theme="@style/AppTheme.Alert"
            android:turnScreenOn="true" />

        <!-- Background Location Service -->
        <service
            android:name=".LocationService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- Phase 5 D-B06 (RL-03): Ambient listen foreground service.
             Play Store stalkerware policy requires this as a microphone-type
             foreground service with a persistent non-dismissable notification
             while capture is active. Service class is authored this phase but
             wired through a Capacitor bridge in v1.1 native-deploy. -->
        <!-- 2026-05-07: foregroundServiceType microphone → specialUse 로 변경.
             Android 14+ 의 mic FGS 는 background 시작 시 foreground UI 요구라
             모토로라 razr cover display 에서 activity launch 가 deferred 되면
             ForegroundServiceStartNotAllowedException. SPECIAL_USE 는 background
             시작 허용. mic capture 는 RECORD_AUDIO 권한 + AudioRecord 로 별개.
             Play Console 검토 시 SUBTYPE 메타로 "remote_listen_cover_display"
             목적 명시. -->
        <service
            android:name=".AmbientListenService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="remote_listen_cover_display" />
        </service>

        <!-- Phase 5.x (force_ring): emergency parental alert FGS + fullscreen Activity.
             specialUse type per Android 14+ requirement; SUBTYPE meta declares
             intent for Play Console policy review. -->
        <service
            android:name=".ForceRingService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="emergency_parental_alert" />
        </service>

        <activity
            android:name=".ForceRingActivity"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:exported="false"
            android:theme="@style/Theme.Hyeni.ForceRing.Fullscreen" />

        <!-- FCM Messaging Service -->
        <service
            android:name=".MyFirebaseMessagingService"
            android:directBootAware="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Boot Receiver: auto-start location service -->
        <receiver
            android:name=".BootReceiver"
            android:directBootAware="true"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.USER_UNLOCKED" />
            </intent-filter>
        </receiver>

        <!-- Phase 0-A: Power-off resilience — 시스템 종료 직전 마지막 위치를 부모에게 push.
             ACTION_SHUTDOWN 은 protected broadcast 이므로 system 만 발신. exported=true 가능. -->
        <receiver
            android:name=".ShutdownReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.ACTION_SHUTDOWN" />
                <action android:name="android.intent.action.QUICKBOOT_POWEROFF" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

</manifest>
